# Proposal: close the known gaps as one spicy-docs program

Status: local stabilization and direct source/output validation on 2026-09-21;
see [the current evidence and limits](legislative-readiness-2026-09-21.md).
The initial validation inventory covered 39 SpicyDocs table contracts over 814
columns and 67 SpicyRegs declarations. The public-artifact observation found 24
available and 43 returning 404. Current FEC integration expands the host dictionary
to 71 declarations; those older public measurements retain their original scope.
Implemented, produced locally, and publicly available are distinct states.
The implementation history below retains its original measurements; historical
uses of "hosted" do not establish current publication. A6 acquisition, C4, C5,
D4, E3 and bill reconstruction remain open in their stated scope.
(revalidated against the trees the same evening: spicy-docs `main` at 9009fd1,
109 commits past v0.21.1, 5,950 tests; written against `main` at v0.21.1
plus docs (5,425 tests), spicy-regs fork branch `billtrax-hosting-prep`
(1,433 tests, 46 hosted tables), and the day's measurements; both trees
have moved since -- each row's status below is the live record). It answers the
GovernmentXML proposal (an external draft dated the same day, not in this
repository) by folding its sound parts into this repository
and replacing its corpus choice with what the census measured. Every gap
below names its evidence, its fix, the measurement that proves the fix, and
the home it lands in: spicy-docs (ingestion, parsing, shared logic),
spicy-regs (hosted tables), upstream (`civictechdc/DeltaTrack`), or the
user (calls only they can make). Nothing here is a commitment until it has a
`docs/decisions.md` record.

## 0. The principle

**Retrieve, then reconstruct, then interpret; never in the other order.** A
body is taken in the publisher's most structured rendition (XML, then HTML,
then text, then PDF as the last resort; measured and sealed 2026-09-19).
Reconstruction runs only where retrieval has nothing structured to give, and
its output is a derivative that says so on every row. Interpretation runs
over facts and findings, names its rule, and never rewrites text. A gap is
closed only by a measurement, and a measurement that could not fail is not a
measurement.

## 1. Where the stack stands

Measured on 2026-09-19 unless stated:

- **Acquisition**: Congress.gov list routes (a route table), GovInfo bodies by
  package id (CRPT, CHRG, CDOC, CDIR, CREC, BILLS) with identity proved before
  bytes, BILLSTATUS bulk zips with a listing-based unchanged skip, USLM laws
  and compilations, press releases, House Clerk and Senate LIS votes, the
  legislators crosswalk, CRS, GAO, CBO, the Court, the Federal Register, CFR
  and eCFR, the Unified Agenda, the U.S. Code, FEC, LDA, SAM, USAspending,
  CourtListener, FCC ECFS, regulations.gov.
- **Parsing**: the bill tree and section diff as adapters over the pinned
  DeltaTrack package, the sealed version-code vocabulary (72 entries, 24
  measured in the 119th), GPO PDF text normalization derived on PyMuPDF and
  measured against upstream's pypdfium2 pipeline, one `body_text` per
  rendition, the agency report-block parser.
- **Interpretation**: stage, money bills, signals, vote, release and member
  matching, section classification, bill and diff summaries with prompts
  sealed by digest, all pure, all naming their rule.
- **Hosting**: 27 contracts over 516 columns in `schemas/` (22 over 407 when
  written; the five Congress.gov index tables landed the same evening), the bill family
  in one pass, activity events from snapshot comparison, six spicy-regs
  rollups incremental where the source allows, a data dictionary reading its
  column prose from the installed contract.
- **Bodies**: 8 source families are PDF-only in practice, 7 are PDF-primary
  with a text alternative now preferred, the rest are structured or
  metadata-only ([census](pdf-only-corpus-2026-09-19.md)).

## 2. The gap register

### 2.0 Execution status (maintained; updated as each branch merges or stalls)

The [consolidated remaining-gaps backlog](remaining-gaps-2026-09-21.md) maps
every original register ID and subsequent validation finding to its current
owner, evidence, next action and completion check. It distinguishes repaired
code from missing source coverage, public adoption and optional deployment.

**2026-09-21 local implementation; public adoption remains open:** H3/H5/H8 are corrected by a
direct public-file observation; the six derived tables remain declared. H6 has
local checkpoint/replacement fixes with failure and raw-PDF replay checks. H10
below records the newly measured cosponsor defect and corrected 118th-Congress
candidate. The installed host now uses SpicyDocs 0.26.0 and Rulespec artifacts
1.0.14. The five-table candidate and retained regulatory repair replay through
those installed packages; Rulespec's opt-in capture v2 remains a separate adoption.
The [repair and adoption record](data-remediation-sprint-2026-09-21.md) retains
direct source comparisons, artifact pins and remaining publication requirements.

| # | Status and dated evidence |
|---|---|
| Consolidated backlog | **2026-09-21: documented and independently reviewed** in spicy-docs `e2c98a8`. The [current backlog](remaining-gaps-2026-09-21.md) reconciles every original register ID, subsequent validation findings, RefSpec/Rulespec owner gaps and new fork operations. It links the FEC task's canonical FG01–FG24 register and complete coverage inventory, pushed in spicy-regs `43c06b6`. Separate operations, FEC and schema/reference reviews passed; raw table/count comparisons caught and corrected the difference between 15 unmapped Senate files and distinct header versions. This is documentation of remaining work, not a new population audit or publication. Review receipts: `~/Work/corpora/fork-cloudflare-2026-09-21/consolidated-*` and `remaining-fec-section-review-approved.json`. |
| H16 lobbying failed-query publication | **2026-09-21: independently reviewed; fix pushed to the fork** in spicy-regs `7551f63`. The existing scheduled job treated an unfiltered HTTP 400 as zero filings and published a 422-byte, 14-column empty table. The invalid family was conditionally withdrawn with immutable evidence retained; the workflow is paused. The reader now supplies a supported cold-start filter and refuses terminal failures, malformed pages and incomplete/count-changing pagination before output/publication. Review independently reproduces both old false-empty and false-partial failures. Two live pages yield three records with 42 native/output cells checked; 63 focused and 2,107 full host tests pass with lint, types and all 71 declarations. The source advertised 1,977,046 filings, about 79,082 pages at 25: initial backfill, interruption/resume, throughput and public data qualification remain open. Literal anomalous publisher dates are preserved. Evidence: `~/Work/corpora/fork-cloudflare-2026-09-21/lda-reader-repair/`, `invalid-family-withdrawal.json`, `lda-final-host-gate.log`. |
| Cloudflare fork storage; bounded SR1 rehearsal | **2026-09-21: configured and independently reviewed** in spicy-regs `dbb9e3e` and `21073f0`. The `mikewolfd/spicy-regs` fork uses `mdeeb@civictechdc.org` account `174055408ff1560e60601c4d12c561c4`, its own `spicy-regs` bucket, named local login and five GitHub storage secrets. Public readers and hosted checks use the selected URL; host changes cannot reuse an old local download. A real two-table synthetic publication passed Rulespec admission, exact public byte comparisons, repeat writes and refusal of a concurrent pointer overwrite. A 6 MiB multipart upload and conditional browser preflight passed; all ten probe objects and unfinished uploads were cleared. Full host gate: 2,067 tests, lint, types and 71-table dictionary; the actual minimal MCP image returns a local Parquet row without ETL dependencies. The rehearsal published no production data. A subsequent scheduled lobbying job exposed a source failure converted to zero records: an unfiltered API request returned HTTP 400, yet the job published an empty generation (zero rows, 422 bytes, 14 columns). The invalid family was conditionally withdrawn, with immutable evidence retained; source failure does not establish absence. Catalog-backed ingestion, a custom domain, source backfill and Worker deployment remain open. Evidence: `~/Work/corpora/fork-cloudflare-2026-09-21/`. |
| SR2 retained regulatory recovery and full RIN sets | **2026-09-21: independently reviewed and merged locally** in spicy-regs `122f540` (recovery `5235ef4`, RIN joins `c00030a`, final integration `f4c4378`). Explicit retained-input repair uses installed source readers and admission, rereads despite old discovery checkpoints, and preserves newer priors and host enrichment. Proceedings and comment periods retain all usable RINs; the House/Federal Register join keeps dated identity. Direct replay covers 391 dockets, 546 documents and three named comments: 10,969 source/output field comparisons, zero mismatches, four newer public observations preserved. It restores 47 docket RIN fields, 496 document attachment fields, 488 Federal Register references and 503 withdrawal values. Review found and fixed temporary-file and index-write retry defects. Final combined gate: 2,042 tests; lint, types and 71-table dictionary pass. Repair is local and per file; ordinary discovery, source-body acquisition, Iceberg, public backfill/publication, lifecycle accuracy and interpretation qualification remain open. Evidence: `receipts/remaining-gaps-wave1-2026-09-21/sr2/`, including `independent-combined-review.json`. |
| SR1; H7/H8 generation admission | **2026-09-21: independently reviewed and merged locally** in spicy-regs `4b34811` (implementation `de7c2a8`, partial-candidate guard `97f1dda`, FEC reconciliation through `28c4cb8`). Complete Parquet families are decoded, schema/count checked and byte-verified through Rulespec before one conditional publication pointer changes. Captured readers, isolated build inputs, complete CLI batches and explicit legacy status prevent mixed or stale managed inputs. The narrow bill writer carries siblings from one complete family; local partial candidates cannot publish. Existing docket-search gzip keeps its legacy path. Full reconciled gate: 2,012 tests; lint, types and 71-table dictionary pass. Native five-table admission preserves all 2,206,611 cells. Live R2 rehearsal, base/partitioned/Iceberg publication, semantic qualification and public adoption remain open. Evidence: `receipts/remaining-gaps-wave1-2026-09-21/sr1/`. |
| RS1 shared capture provenance | **2026-09-21: independently reviewed and merged locally** in Rulespec `3c3bd79b` plus replay-command correction `f15fae69`. Opt-in DocumentCapture v2 shares acquisition/MODS evidence, derived-artifact identity/page maps, effective coordinates and rule/converter bindings. V1 schema bytes and default APIs remain unchanged. Independent replay preserves 934 nodes and 2,444 spans across seven captures; 28 capture tests, installed-wheel checks and 44 canonical cases pass. The 11 original provenance omissions remain findings, plus explicit archive/member work pending RS2. CFR original PDF bytes and acquisition time remain unavailable/unproven. Rulespec-artifacts 1.1.0 is prepared locally; source v2 adoption and release remain open. Evidence: `receipts/remaining-gaps-wave1-2026-09-21/rs1/`. |
| SD1 shared table validation | **2026-09-21: reviewed and merged locally** in spicy-docs `b3b59e0`. JSON serialization refuses nested non-finite values as named bill-family row refusals; finite JSON bytes and all 2,206,611 cells in the retained 118th HR/S five-table replay are unchanged. Independent review reopened native H.R. 1 XML and actual output and matched all 52 subjects. The full source gate passed 7,263 tests. Digest versions now explicitly establish equality, not chronology; per-column logical types and mixed-rule host inputs remain open. Evidence: `receipts/remaining-gaps-wave1-2026-09-21/sd1/`, including independent review and raw-input/output witness. No new package release or public adoption. |
| Validation sprint; H11–H15 | **2026-09-21: complete inventory and evidence review, committed locally** through spicy-docs `d548ec8`. [Sprint verdict](data-validation-sprint-2026-09-21.md) and [full scorecard](data-validation-tables-2026-09-21.md) cover all 70 emitted/declared tables and 20 source families. Structural profiles use 23 whole public files, a 212,733-row comment sample and 46 local files; source comparisons retain their named scope. Independent review corrected lifecycle denominators before approval. CFR suffix preservation is fixed in spicy-regs `cb75404`; public adoption remains open. Full gates: 7,251 source and 1,850 host tests passed; existing unrelated host formatting debt is disclosed. No push, release or deployment. |
| H12 Federal Register identity | **2026-09-21: host and four materialization families fixed locally** in spicy-regs `5587315`. Number/date keys preserve distinct records; dated downstream joins retain ambiguous number-only observations. Native daily replay preserves 237/237 rows; all 957 dated census keys survive. Full pinned-file repair adds only `00-111@2000-01-14` (803,996 → 803,997), with no lost keys or changed unrelated source fields. Independent review approved after 5,451 exact native field comparisons and 31 focused checks; full branch gate 1,861 passed. Historical backfill and coherent publication remain open. Evidence: `receipts/remediation-sprint-2026-09-21/fr-identity/`; the exact-number consumer witness is `native-consumers-results-verified.json`. |
| H13 court text variants | **2026-09-21: host fix committed locally** in spicy-regs `d641411`. The version-2 artifact retains all eight native strings, preserves empty versus NULL, and derives availability from stored values. One pinned 2 MiB native bulk prefix produced 284 rows with 2,272 exact field comparisons; independent CSV reading confirmed the discarded HTML witness. Legacy priors require an explicit rebuild; no existing artifact is claimed repaired. Independent review approved; full host gate 1,891 passed. Evidence: `receipts/remediation-sprint-2026-09-21/court-text/`. Full selected-scope rebuild and public adoption remain open. |
| G5 owner-package adoption | **2026-09-21: installed validator adopted locally** in spicy-docs `8303afa` and replay commit `169438f`. Rulespec 1.0.14 supplies the unchanged parent and profile schemas plus corrected invariant checker; three copied owner files and fallback are removed. Seven retained captures replay with identical source bytes, structure and text; both owner counterexamples fail as intended. Independent review approved; full source gate 7,252 passed, four skips. Existing source-provenance gaps G3/G4 remain open. Evidence: `receipts/remediation-sprint-2026-09-21/rulespec-adoption/`. |
| Local source package | **2026-09-21: SpicyDocs 0.25.0 prepared and adopted locally** in `1c86f16` and spicy-regs `230cce5`, including H10 cosponsors, report headings and Rulespec 1.0.14 adoption. The wheel matches all 264 packaged source files, exposes 39 tables over 814 columns, and passed the integrated 7,255-test source gate. Installed report and bill replays qualify the named cohorts below. No external release, tag or push. Evidence: `receipts/remediation-sprint-2026-09-21/source-reader-adoption/docs-0.25.0/`. |
| Installed host adoption | **2026-09-21: committed locally** in spicy-regs `230cce5`. CRPT checkpoints include the installed reader and rule versions; successful reads replace their complete section set, including empty results, while failed/unread reports retain prior rows. The installed-reader replay corrects eight real report blocks outside discovery with an unchanged publisher timestamp and skips the second read. Independent review approved; combined host gate after integrating H12/H13 passes 1,905 tests, lint, types and the 67-table dictionary. Evidence: `receipts/remediation-sprint-2026-09-21/source-reader-adoption/`. Public rebuild remains open. |
| Report headings | **2026-09-21: source and host fixes committed locally** in spicy-docs `a7519d6` and spicy-regs `230cce5`. `report_sections` retains literal headings and leaves unresolved agency identities NULL. Complete CRPT-119hrpt796 HTML and a rendered native appropriations page preserve all existing non-agency cells (110 comparisons across 11 blocks); nine literal headings survive. Independent review approved; initial source gate 7,254 passed. Installed host replay preserves 80 existing non-agency fields across the real report's eight blocks. Public rebuild remains open. Evidence: `receipts/remediation-sprint-2026-09-21/report-headings/` and `source-reader-adoption/report-replay/`. |
| H6 | **2026-09-21: committed locally** in spicy-regs `e7cc6f3`. Print and Senate corrections use successful-read checkpoints, replace successful empty results, preserve failed reads, retain prior print text digests, and revisit stale files outside discovery. Independent review approved; 107 focused and 1,846 full host tests passed. Public deployment and H7 remain open. |
| H10 | **2026-09-21: source fix and host adoption committed locally** in spicy-docs `7335034` and spicy-regs `230cce5`. Native cosponsors are counted separately from sponsors; a retained five-table 118th HR/S candidate passes 1,189,950 named source-field comparisons. Installed-package replay matches all 2,206,611 candidate cells by stable keys and repeats the native checks without a source-path overlay. Initial source gate: 7,251 passed. Wider native-input replay and coherent public generation remain open. Evidence: `receipts/remediation-sprint-2026-09-21/source-reader-adoption/bill-replay/`. |
| H3, H5, H8, H9 | **2026-09-21: evidence and wording corrected locally** in spicy-regs `23d32c9` and spicy-docs `f20904d`. All six supposedly unproduced derived tables exist publicly; the expanded bill and subject outputs remain local; declared schemas and dates do not establish publication; measured interpretation precision does not certify individual findings. Automated artifact admission and interpretation qualification remain open. |
| A1, A2, A3 | Landed on spicy-regs `billtrax-hosting-prep` (merge 1f18898), twice reviewed. The linkage receipt corrected the hearing figures to 10 hearings with mentions in 31 entries; the load-bearing 12-of-12 report agreement and 0-of-50-distinct-hearings PRIMARY (52 fetches) held throughout. The `press_releases` NULL-match exposure is a documented residual, not a refusal. A2's PRIMARY-context rule is still spicy-regs's own `_mods_bills` copy: the vendored 0.21.1 wheel has no `PackageModsIdentity.primary_bill`, so the copy is deleted on the 0.21.2 vendor bump. **A2's hearing half reopened and re-measured 2026-09-20** ([`hearing-bill-linkage-2026-09-20.md`](hearing-bill-linkage-2026-09-20.md), 198 requests, receipt `hearing-bill-linkage-2026-09-20/`): the 0-of-52 PRIMARY and 0-confirmed BODY findings both hold, but they were the wrong question -- a hearing is held on a *list*, and the MODS `<bill context="COVER">` the body acquirer already fetches states it. Six set-comparisons against independent sources (the Congress.gov hearing title x3, the Daily Digest committee entry, `relatedItems.bills`, the CHRG front page) are **6 of 6 set-equal over 55 bills**, and the bill side's *Hearings Held* action confirms **19 of 20** COVER pairs against **0 of 23** BODY-only mentions. The prior **0 of 12** on `relatedItems.bills` was a skewed frame, not the field: sampling the meeting list route directly gives 10 of 60 meetings stating a bill (2 of 29 House hearings, 3 of 13 Senate). Recommendation: leave `hearing_transcripts.bill_id` NULL for a **new reason** (one-to-many, not "no source states it") and add a link table in the `fr_docket_links` shape with `link_rule` and `link_source` per row. Open: the COVER census per Congress and chamber -- 3 of 3 sampled Senate CHRG MODS state no bill in any context. **Landed 2026-09-20** (`hearing_bill_links`, from commit 396f456; the merge hash lands with the merge): the link table is keyed `(package_id, bill_id, link_source)` with `relation` sealed to `held_on`/`noticed` and a derived `link_rule_version`; `mods_cover` and `docs_house_br` are implemented and `daily_digest_entry`, `congress_related_items` and `front_matter_designator` are named in the sealed vocabulary, measured and unfilled. `hearing_transcripts.bill_id` stays NULL for the new reason and `shape_hearing_transcript` lost its `bill_id` argument so the code cannot contradict it; `PackageModsIdentity` gained `held_date`. The registry is **thirty-eight contracts over 778 columns**. Reproduced from the retained bytes through the product rules, **48 of 48 comparisons, 0 requests** (`tools/analysis/hearing_bill_links_recompute.py`, receipt `hearing-bill-links-build-2026-09-20/`): the 20 per-hearing COVER sets per bill and both directions, the six set-comparison counts and their 55-bill total, 46 BR documents with 36 resolving, and a presence check so an absent MODS file cannot read as an empty COVER set agreeing with an empty one. Still open and unchanged: the per-chamber, per-Congress COVER census; the 19-of-20 and 18-of-18 bill-side confirmations are not reproducible offline, because `probe3.json` retains them scored rather than as action lists. The spicy-regs rollup is not started. |
| A4 | Landed in spicy-docs: the Senate LIS vote-menu index (`VoteAcquirer.list_senate_votes`, `parse_senate_vote_menu`, `locator_from_menu_entry`). Hosting still open (validation 2026-09-20): spicy-regs's roll-call rollup selects House votes only, so the Senate index is acquired and not yet hosted. |
| A5, A7, A10 | The route table they sit on landed at 1f515bb (three review rounds). The contracts landed after review (APPROVE; two prose nits applied): `house_communications`, `committee_meetings`, `record_issues`, `treaties`, `nominations`, `hearing_transcripts.event_id`, `interpretation/communication_rin.py` -- 27 tables over 516 columns. All six flagged decisions upheld; the disclosed limits (18 of 25 sampled, one hearing/meeting pair, cross-chamber uniqueness unmeasured) are stated in the docs. Receipts: an 18-detail RIN sample (18 dated referrals, 12 rulemakings, the same 12 with a RIN) and a 2-request meeting-edges run (hearing 64431 <-> meeting 119003, jackets both ways). `senate_communications` deliberately omitted: the Senate detail lacks the bridge's fields. **A7's `event_id` key measured against the House Committee Repository 2026-09-20** ([`hearing-bill-linkage-2026-09-20.md`](hearing-bill-linkage-2026-09-20.md)): `associatedMeeting.eventId` **is** docs.house.gov's `EventID` -- 9 of 9 resolved, and all 9 passed a committee-and-date identity check rather than a status check, so the keyless per-event meeting XML (`<meeting-document type="BR">`) is reachable from the contract's own key. Two limits found: only 9 of 20 sampled hearings state an `eventId` at all, and the meeting-to-jacket edge is many-valued with 1 of 5 pointing at a transcript that names no meeting -- so nothing may attribute a meeting's bills to a transcript without the hearing's own reverse edge. The Federal Register `rin` join column is spicy-regs rollup work; no acquisition-policy change was needed. The spicy-regs rollups landed on `billtrax-hosting-prep` (merge a431fef, reviewed: REQUEST CHANGES on four small items, all applied with tests): five tables through one parameterised transform with whole-list walks, page-boundary repeats collapsed and logged as a shortfall the next walk recovers, details capped per run with the contract's NULL-vs-[] rule as the resume marker; the Federal Register table gains `rin` (96,060 one-RIN documents, 1,499 with more, command and output retained); committee reports read the hearing detail for `event_id`. 54 hosted tables. **A7's `event_id` key is now used by product code (2026-09-20, from commit 396f456)**: `sources/congress/house_committee_repository.py` reads the per-event XML as a keyless GET at the static `/meetings/{CMTE}/{SUBCMTE}/{yyyymmdd}/{EventID}/...xml` address (byte-identical to the page's own `__doPostBack` download, 2 of 2 by SHA-256, checked by digest because this publisher serves its own pages at 200), and `interpretation/hearing_bill_links.py` runs the committee-and-date identity check **per row** before any agenda bill reaches a transcript -- the two limits this register recorded are honoured rather than argued away. The reader refuses a type-less `<legis-num>` instead of defaulting it to `hr` the way the community scraper does. Still open: why 11 of 20 hearings state no `eventId`, how far back the Committee Repository reaches, and whether the per-committee RSS feed enumerates more than a rolling window -- none probed. |
| A6 | Measured and landed with the map merge: the 370-page walk histograms all 92,450 requirement communications at roughly a 29 percent detail-era share, under the stated 50 percent useful-share threshold, so `house_requirements` is not hosted; the histogram and the detail floor are written into the map. **Reopened 2026-09-20 by measurement, not by argument** ([executive-communications backfill](executive-communications-backfill-2026-09-20.md), 40 requests): the 114th-Congress `house-communication` detail `abstract` **equals** the entry the Congressional Record printed in its House `EXECUTIVE COMMUNICATIONS, ETC.` section, under named normalizations -- four of them, once the overlap run found the fourth -- and every other typed field is a span of that same sentence. The section is a titled `granuleClass: HOUSE` CREC granule with HTML on every one of 7 sampled issues spanning the 104th-114th; 216 entries parsed at 216/216 for the opening, the `transmitting` split and the committee referral, 210/216 for the cited authority, and 87/216 for a RIN under `interpretation/communication_rin.py` unchanged. The detail floor itself was re-derived against the publisher's own uppercase locator (`house-communication/112/EC/2` 404, `114/EC/4329` 200), so A6's floor stands while its *conclusion* does not: the pre-114th details are recoverable, and the recommendation is to backfill into `house_communications` with provenance columns rather than to leave 65,725 communications unhosted. **Scored and landed 2026-09-20** ([overlap measurement](record-communications-overlap-2026-09-20.md), 296 requests: 40 GovInfo of 40, 256 keyed Congress.gov of 600, both caps declared first and neither moved after). The parse rule is product code (`sources/congress/record_communications.py`, versioned `record-communication-dceeb0d93d03`, all four §3 failure modes handled and the §6 contiguity witness included), and it was scored field by field against the publisher's own decomposition on ten sampled House sitting days of the 114th-118th, 256 entries, every one with a detail record. **The first score was 58.4% on `abstract`** -- two ground truths had agreed and 166 rows did not -- and three GPO print artifacts explained it (the hyphenated line wrap `extraction/gpo_normalize.py` already owns for PDF text, a fourth publisher normalization `Pub. L.`, and a print dash the Record spells with four hyphens). A fourth finding came from the sections rather than any field comparison: **the 117th and 118th parsed to zero entries**, because the Record numbers an entry `EC-1205.` from 2021 and `1205.` before it. Held-out precision after the fixes (the 116th-118th, 144 rows, no field rule ever revised against them): `abstract` 97.9% against a declared 95% floor, `legal_authority` 99.1%, `rin` 100%, `report_nature` 95.8%, `referral_count` 95.1%, and the official/agency pair on its one declared denominator (the rows the rule answered and the publisher decomposed) **failing on both sides, `submitting_agency` 88.4% and `submitting_official` 85.3% against a declared 90% floor** -- scored per side instead they read 94.2% and 90.9% over 121 rows, and the whole 8-row difference is one granule where the publisher put the entire from-clause in `submittingAgency` and stated no official. Ten of ten issues contiguous and strictly increasing, no holes. So the contract changed and the split did not publish: `house_communications` gains `source_route`, `record_package_id`, `record_granule_id`, `record_entry_text` and `reconstruction_rule_version` (32 columns; the registry is 37 contracts over **771** columns), with the §5 rules written in -- a reconstructed row's `url` NULL, the merge preferring provenance over `update_date`, and `submitting_official`/`submitting_agency` NULL beside the retained sentence (the referral's printed *names* do publish; `referral_system_code` does not, because the name-to-system-code resolver is unbuilt). Decision record landed. Still open: the acquisition run itself (about 2,800 granules, roughly 11,000 GovInfo requests, detached and resuming from its own output), the split-part granule `CREC-2004-06-16-pt2-PgH4285`, contiguity across a whole Congress, and the committee-name to `committees.system_code` resolver every reconstructed row's `referral_system_code` waits on. |
| A8, A9 | Landed (merge f86715f) after two review rounds: `laws`, `law_code_sections`, `table3_records`, `committees`, `committee_assignments`, the OLRC classification source and the two chamber roster readers, with receipts under `laws-contract-2026-09-19/`, `olrc-classification-2026-09-19/` and `roster-comparison-2026-09-19/`; the registry is thirty-two contracts over 617 columns. The spicy-regs rollups landed on `billtrax-hosting-prep` (merge fdc3d8e, reviewed: three blockers and two fixes applied with tests): laws, code sections and Table III by one rollup, committees and assignments by another; the Statutes at Large citation fills `congress_bills` by a left join on the published laws after every merge, tolerant of a corrupt laws file; the committee route's over-declaration read past under an absolute bound of eight; Table III steps past a refused act and stops after three consecutive refusals. Every contract 0.21.2 ships is now hosted: 59 tables, unhosted set empty |
| A11 | Landed on spicy-regs `billtrax-hosting-prep` (merge f9a1a64) after two review rounds: the family rollup backfills the 82nd-107th per (congress, bill_type) through the shared bill-route reader under the same per-run cap, pages and details both charged, every attempt retained with its refusal so refusals are retried first and a complete unit settles; backfilled rows marked by NULL `schema_version` with every unexamined column NULL. Proof: the 92nd walked end to end, declared 767, walked 767, 82 keyed requests, receipt retained; publisher finding: Congress.gov lists hr 4634 twice across a page boundary, so declared totals count entries, not bills. |
| A12 | Landed first on main with the map's own hardening (branch `worktree-agent-ad19194ecd76ab500`, reviewed APPROVE: every `have` row verified against live `LIST_ROUTES`, `check_evidence` hardened so a `have` row must name a real route and a defined symbol). Refreshed offline on `map-refresh-2026-09-20` in commit `c120699`. **Eight rows promoted to `have` (five `candidate`, three `rejected`)**: CPRT bodies, House MemberData.xml, Senate committee XML, OLRC classification tables, the House committee agenda reader, House activity reports, Senate expenditures and President's Budget volumes (thirteen measured parts). The combined rows were split: House floor documents remain `candidate`, agency CBJs remain `rejected`, and **CRECB bodies move from `have` to `candidate`** because `govinfo/bodies.py` has no CRECB grammar and `parse_package_id` refuses that prefix; daily CREC package/granule bodies remain `have`. Existing `have` notes now describe CBO estimates, hearing-bill links, Record executive communications, CRS HTML preference and print-action rows, with their limits. Markdown and sidecar rows agree; all retained measurements are unchanged. `check_evidence` passes and the focused map suite passes **18 tests**, including byte comparison and offline-refresh checks. Local commits only; no network request or push. |
| A13, E4, E5 | Built and reviewed APPROVE on branch `worktree-agent-ad19194ecd76ab500` (extended route floors, the weekly live-publisher workflow, the README index test); landed on main. The same work hardened `check_evidence` to check named listing routes and defined symbols. E4's Federal Register drift was triaged first: a stale pin, not publisher drift (merge b3d00f5). |
| B1 | Sized: the held-out measurement merged at 8c0e023 -- section recovery 170 of 170 out of sample (100 percent precision and recall), quoted-block macro recall 83 percent (the one fitted rule), 141 requests with a retained receipt. The reconstruction pilot (§3) landed after two review rounds (the second verified the re-scored run from its artifacts, including a digest-identical corpus manifest and the 84-request log): rules frozen at da96d21, fetched and scored in one pass at the same 40 granules -- text 99.93/99.96, hierarchy F1 1.0000, schema and coverage clean, acceptance 78 percent, zero model calls. The bill_dtd profile on pre-113th HTML is the next work. |
| B2, B7, A10 (the CPRT row), the ModsBill accessor | Landed after review (APPROVE; the census arithmetic re-derived independently by the reviewer; two nits folded in: isdecimal guards, and the 400 body-shape condition stated where the rule is compressed). The decision record landed; the USLM proof was re-derived and measured: the census's apparent ten entries are 5 BILLS packages (hconres11 double-pinned under enr and rds; three more bills multi-pinned; the other 4 entries are PLAW collection packages outside B7's scope), all five fetched live and parsed, roots measured on two bill types (`<bill>` and `<resolution>`). Both receipts written with the not-retained-log statement. |
| B3 | Landed: the CRS HTML rendition is preferred for the current version, with the preference structural rather than a passed-arg convention (merge 4e9ab7c). |
| B4, B8 | B4 reopened with a target on 2026-09-20 (the owner: roll the PDF families up with the keys that relate them to other tables; never recreate what an index states; leave nothing of value only the PDF holds). The yield measurement (`pdf-family-rollup-yield-2026-09-20.md`, merge 6cea782, 118 requests) was **corrected the same day** by `pdf-yield-mods-recheck-2026-09-20.md` (24 keyed requests, three GovInfo collections, every page of 24 retained PDFs re-read): it had compared each print against GovInfo's `published` listing row, not against the package MODS `GovInfoBodyAcquirer` fetches for every body it reads. Restated as print-only against the MODS, the activity reports' **883 bills and 75 laws are 0 and 0** — uncapped, 0 of 1,406 bills and 0 of 174 laws, the single apparent survivor (`188-11`) being a print-side false positive. The MODS also states the submitting member's `bioGuideId` (7 of 8) and 374 report-to-report references, so both are ingest, not extraction. **Build order, reordered**: `document_citations` on the **budget volumes** first (504 of 518 public laws are print-only on a full read, against 43 of 69 capped); then a MODS loader for the CRPT-stated keys; then the narrowed activity-report print contract (87 RINs, 37 dockets, 27 committees, 5 GAO ids). `senate_expenditures` stays the table contract, citation yield zero. CRS unchanged. CBO stays blocked (the proxy's 520s are transport failures, the census's 403s the refusal; a feed-only bill-to-estimate link is buildable). CourtListener does state the outbound opinion edge (`opinions[].cites`), which the rollup dropped from the record it compared. **Blocked on a decision record**: `bodies.py`'s package-id grammar reaches neither `BUDGET-*` nor `GPO-CDOC-*`, so neither family is acquirable in product code until it is widened. The Zyte transport joined `spicy_docs.transport`. B8 stays deferred. Landed the same day on the narrowed yield: `interpretation/citations.py` (the sixteen measured rules as versioned named rules, one home shared with the measurement tools), the `document_citations` link contract keyed on document, text digest, kind, target and span, and `house_activity_reports` with the MODS-stated bills, laws, code sections, member and related reports as its index columns; the registry is 34 contracts over 670 columns. Both measurements and the first contracts are on main (merges 6cf63c5, e971cb9, reconciled at 5f2f0a6: one rule set, both sidecars regenerated, the budget committee count ten to nine under the guarded resolver, the activity reports' surviving law count zero). Landed (merges 744b937 and f02daec, fix-forward 1 commit): the package-id grammar widened to BUDGET and GPO-CDOC with the `budget_volumes` contract, and the `senate_expenditures` contract; the registry is 37 contracts over 766 columns. Landed: the sealed package-id grammar widened to BUDGET and GPO-CDOC (measured on the 24 retained MODS; decision record) and the `budget_volumes` contract (34 columns; citations through the link contract as `budget_volume`; the family's bill yield published as a congress-blind count, 6 print-only of 8). Landed: the `senate_expenditures` contract (35 columns keyed on package, file, page, table, row and text digest; cells verbatim with the header band's roles; the totals check reconciles both volumes to the cent; a multi-year funding-year spelling that would have misattributed five pages of rows fixed with an independent reader agreeing at 83 of 83). **The last open CRPT question is now closed and it landed** (merge c9a54c1; `bill-action-relationship-2026-09-20.md`; every figure from retained bytes except one bounded 20-request probe). The print's bill-action relationship is **hosted** (spicy-regs merge bdedd8f, 12,700 rows in the measured run): `bill_committee_actions` (27 columns, keyed on document, text digest, bill, print phrasing and the phrase's own span), shaped from a new `interpretation/bill_actions.py` whose 25-phrasing vocabulary is sealed and additions-only and whose rungs are derived from `bill_stage` rather than declared beside it. 4,456 rows over 978 bills from 1,249 pages. **A published row is the right kind *and* the right bill 83.3% of the time in the single-bill class (30 of 36 hand-checked) and 50% in the multi-bill class (2 of 4); 4,089 of 4,456 rows (91.8%) are single-bill**, so `WHERE attachment_confidence = 'single'` is the trusted view at an 8% cost in volume and the `multi` rows stay as evidence to verify. Recall is a separate axis and is 59.6%. **This measurement was wrong about the same thing twice, in opposite directions, and the mechanism was identical: a check validated against a record that was not the publisher's answer.** First it took `72` Hearing held in House and `74` Markup in House for action codes — both are section-5 *LOC summaries version codes* — and its self-check scanned the whole guide, validating against a 123-code superset. Scoped to section 3 it then concluded the publisher has **no** House hearing or markup code and the print is the only structured source; **that is false on the wire**. Section 3 says in its own first paragraph that it is representational and that no authoritative list exists, and 13 of the 35 distinct codes in the 20 retained responses appear nowhere in it — including `H21000` Subcommittee Hearings Held and `H15000-B`/`H15001`/`H22000` for markups. The comparison that should have caught it asked only whether the publisher states a code *this repository maps the phrasing to*, which for those two was empty by construction, so `code_matched == 0` was an identity and a test asserted it; it now reads the publisher's codes off the response, matched on wording. **Restated on the bytes**: the publisher has no counterpart at all to **10 of the 15** subcommittee hearings these prints state, the 5 it does state come from two bills and both carry `H21000`, and all **8 of 8** markups are stated and coded. So the print is often the only record of a subcommittee hearing and a second, coded source for a markup; 280 further rows name events no known code covers. Backlog, all request-free against the same eight prints: anaphora (“the bill”), list headings, the per-committee entry unit, the en-bloc disposition (must be scored held-out), ruled-table column headers, and scoring the extracted dates. Acquisition-gated: the corpus-wide row-for-row `bill_actions` overlap (20 of 978 bills done) and the pre-108th case — no CRPT artifact outside the 118th and 119th is retained anywhere, and one `published` walk of the `CRPT` collection with an early `dateIssued` window would establish whether it reaches earlier Congresses at all. CBO routes measured 2026-09-20 ([`cbo-cost-estimate-routes-2026-09-20.md`](cbo-cost-estimate-routes-2026-09-20.md), 130 requests, receipt retained, verified): the cost-estimate index is keyless in the BILLSTATUS bulk zips (1,368 bills of the 118th, 1,468 estimate rows, identical to the API on 33 of 33 overlapping bills), the letter text is reprinted in CRPT bodies gated on the cover recital (ceiling 883 of 1,368, 64.5 percent; the summary cost card is a raster in every rendition), and Zyte's own error names a website ban on 5 of 6 rendered calls, so the wall stays and the build order is index, then reports, then the unreported remainder as a stated gap. Committee-action sources measured 2026-09-20 ([`committee-action-sources-2026-09-20.md`](committee-action-sources-2026-09-20.md), 140 requests, receipt retained, verified APPROVE): the bill link lives on the meeting record, not the bill's, and is populated for 2 of 2 markups and 1 of 1 Meeting but 0 of 13 records filed as Hearing across Congress.gov and docs.house.gov; the Daily Digest stated 0 of the 10 target events; the wire carries 15 action codes in no published list (4100/4200 exist only in congress.gov's search vocabulary, never on the bulk feed's 90 observed records). Plan: `bill_committee_actions` gains a `source` column fed from committee-meeting joined to the repository XML, with the print kept as the only record for events filed as hearings. **The three follow-ups the first hosted run exposed are landed in spicy-docs** (commits 750eb94, 1a59cfd, ac8d691, c3edee2; gate clean): the sealed BUDGET part vocabulary widened from six to **thirteen measured parts** on the run's own refusals -- `OBJCLASS`, `TAB`, `DB`, `CLIMATE`, `LRB`, `CROSSCUT`, `DOD`, the parts carried by all **17 of the 40** rows a 2023-01-01 `published/BUDGET` walk served that the sealed six refused by name -- each added with the id that showed it and proved on its own summary and MODS (14 keyed requests against a cap of 20 declared first, receipt `budget-parts-2026-09-20/`), with the vocabulary still sealed and additions-only; the CRPT activity-report title rule moved out of `tools/` into `sources/govinfo/activity_reports.py` with its rejected bare-word alternative and a derived version, so the host's restatement can be deleted (re-derived request-free: 71 walked, 20 bare-word, **15 matched**, and the two real activity reports it misses are published as the floor's reason rather than widened away); and `PRINT_BODY_PREFERENCE` joined `bodies.py` as a permutation of the sealed order with PDF first, for the page-stating print families -- `BODY_PREFERENCE` unmoved. One publisher fact the widening exposed: an address is not a body, and 4 of the 7 new parts state no PDF at the package root (3 state one only inside a constituent, at a granule stem). **The CBO capability landed 2026-09-20** ([`cbo-cost-estimates-build-2026-09-20.md`](cbo-cost-estimates-build-2026-09-20.md); receipt `cbo-cost-estimates-build-2026-09-20/`; **zero requests**, a 10-request keyed cap stated first and unspent, every number re-derived offline from the routes receipt's own bytes *through the product code*). **Step zero decided the shape**: `parse_bill_status` read neither `<cboCostEstimates>` nor `<committeeReports>` and `docs/tables.md` had no occurrence of `cbo`, so the index is a **new** contract rather than columns appended to `congress_bills`. Landed: `cbo_cost_estimates` (16 columns keyed `(bill_id, publication_id)`, filled in the bill family's same one pass off the same document, publication id parsed by a rule that refuses every url outside the measured `/publication/{id}` page); thirteen columns appended to `committee_reports` for the reprinted letter; and `interpretation/cbo_estimates.py`, the named versioned rule gated on the **cover recital** with each pattern's reason and rejects beside it. Reproduced exactly: 16,213 bills, 1,368 with the element, 1,468 items, **1,431** distinct publication ids as a union across both zips, 0 empty elements, 0 urls outside the shape, 1,431 published rows all keying uniquely, and the **883 of 1,368 (64.5 percent)** text ceiling now readable off one row. 17 CRPT bodies: 7 recitals, **7 of 7** letters located (6 at the Director's attribution, 1 at the next heading in its series), 6 naming a signatory, 4 publisher reasons carried whole. Three findings the build added to the routes note: the publisher states one publication **twice** on 37 of 1,468 items and **9 of those disagree**, every one in `title` alone, so the fold keeps them in `restatements_json`; the part citation is spelled `H. Rept. 118-167,Part 2` with **no space**, which a first pattern read as 12 unparsed rows (now 0 of 910); and across all 17 bodies there is **one** `cbo.gov` locator (a footnote to an unrelated study), **no** `/publication/{id}` page and none inside a letter -- which is why the relation is a bill join and **not** a `document_citations` row, and why the letter does not sit on the estimate row (61 scored bills carry more than one estimate, 28 of those also carry a report). The 7 committed excerpts were re-run against the whole bodies they were cut from, **7 of 7 agreeing**. Registry now **38 contracts over 796 columns** (including the appended bill outcome). Still not claimed, and stated: no cost figure from either table (the summary card is a raster and no extraction was attempted), no letter date (0 of 17 bodies carries a letterhead dateline; `pub_date` on the index row is the date), and the ~35 percent of scored bills with no report keep a locator and a recorded absence. **Review corrections:** append nullable `congress_bills.cbo_cost_estimates_outcome` so unread, populated, requested-empty absent, present-and-empty and named unexpected shapes survive without estimate rows. The first receipt hard-coded the empty count and could not see an empty element; a mixed-shape synthetic zip now detects that defect. Re-reading the retained zips made zero requests: **1,368 populated, 14,845 absent, 0 empty, 0 unexpected; 1,468 items and 1,431 uniquely keyed rows**. Refused estimate URLs now retain only the rule, host and path shape; free text is credential-scrubbed before truncation. Zero report citations means this capture names no report, not that no CRPT package exists. **PDF support chosen:** CRPT-118hrpt53, -118hrpt276, -118hrpt930 and -118srpt289 now yield **4 of 4** pinned letter spans through `rendition_text`, using whole uppercase headings and bounded wrapped attributions. The research PDF preference is supported; all **17 of 17 HTM findings** stay identical except version. Rule `cf790f0f814a` includes every pattern, flags, rejects, heading threshold and control-flow revision, with a literal pin and mutation assertions. Paragraphs are scanned once, letter digests use the shared helper, and differing restatements accumulate before freezing. The decision entry and build note record these choices and the corrected receipt. |
| B5 | Landed: `TableObservation` in the evidence model, opt-in via `tables=True` (merge 1477d84). |
| B6 | Landed: the 42-document corpus validation (merge b16c77c); two `_layout_verdict` false negatives fixed; the widened rule's blast radius pinned by a multi-page test. |
| C1, C4 | C1 ran inside the D1 pass and **found a defect**: the sealed `v1` summary prompt asked for its three items in prose and named none of the JSON keys its reader requires, so the model answered and the reader refused -- a keyed run would have published zero `bill_summaries` rows. All three prompts moved to `v2` (they now state the JSON shape their reader parses, from one declaration), and the fix was proved live: three keyed calls, one per prompt kind, receipt `~/Work/corpora/supply-2026-09-02/receipts/c1-prompt-fix-2026-09-19/`. Summary and diff summary are answered and read into rows. **Continued 2026-09-20**: the shape is now a constraint and not only a request -- `answer_schema` derives a draft 2020-12 schema from the same `AnswerField` tuple, `ModelCall` carries it, and the adapter (moved in-repo to `interpretation/gemini_call.py`) sends it as `responseJsonSchema`, where BillTrax's zod schemas sat; prompt bytes and all three `v2` digests unchanged. One keyed call over the same batch (`c1-classification-2026-09-20/`, 263 in / 80 out, USD 0.000279) **settled the classification refusal**: the answer honoured the schema in every respect it constrains and the batch guard refused it again, because the model returned each id with the prompt's own square brackets around it (`[introduced-in-house\|govinfo\|0]`) -- `section_block` shows `[<id>]` and the field asks for the id "copied exactly as given below". A keyed run under `v2` publishes zero `section_classifications` rows. **Fixed and proved the same day**: `v3` rewords that one field and nothing else (`section_block` and `_read_row` untouched, `sectionId` still unconstrained on the request), and two identical keyed calls (`c1-classification-v3-2026-09-20/`, prompt `411e9f67…`, 534 in / 200 out, USD 0.00066) each answered in the batch's own ids and each stored three rows. **All three model-backed modules now produce live rows**; the `v2` answer stays a committed fixture so the wording cannot regress. Also fixed, found by spicy-regs adopting 0.21.3: a `ModelCallError` used to abort the whole `build_bill_family` pass and is now a named `FamilyRefusal`. C4 not started. |
| C2, C3 | Landed, with post-merge review fixes: C2's measurement is the cited BillTrax configuration and the MySQL 8.4 manual, encoded and tested; C3 has the run receipt; the fabricated 40,260-row claim rewritten, the Federal Register locator claim actually asserted and its inputs pinned. |
| C5 | Reopened 2026-09-20 by the validation: the no-change closure addressed hyphen-wrap fragments, which the HTML rendition removed, but the row names all-caps title-page sentences read as block headers, and that defect is still documented in `docs/sources/agency-report-blocks.md`. It stays open as a documented parser limit until a bounded count over the corpus measures it. |
| D1, D4 | D1 landed on spicy-regs `billtrax-hosting-prep` (merge bc82c51, reviewed: every headline number re-derived from the raw ledgers and parquets): all thirteen rollups once under their production caps, 36 tables written, the dictionary's coverage statements carrying the numbers and the cold-start limit, `measured_on` restated. Findings: a private-law printing aborted the bill-family run (fixed, both guards mutation-checked); OLRC Table III truncates after a 200 (re-derived with a control act); 20 of 419,621 bill rows carry no latest action date. The second run, which measures the held-row and unchanged-zip skips, is the cheapest measurement still open. D4: the hearing-transcript fixture is still a CRPT body under the real CHRG-119hhrg64431 identity, disclosed in `docs/tables.md`. |
| D2 | Landed (SR01: the bill reader walks spicy-docs's listing route). |
| D3 | Waiting on the user: re-vendor after the fork merges upstream. spicysearch pins the 24-table catalog `01c77a4a…`; the hosting branch's catalog is 59 tables (digest `0952553b…` per the 2026-09-20 validation; A11 merged). |
| D5 | Measured and landed with D1: 5,348 keyed requests over the night, worst rolling hour 3,680 against the 4,000 ceiling (320 to spare, 2,400 of it the bill family run twice); under the cap, so the eight archive listing reads stand. |
| E1, E2 | Open; the user files the upstream issues. |
| E3 | Measured, not gated: `ty check` on main finds 1,139 errors (553 invalid-argument-type, 517 unresolved-attribute, then a thin tail; 1 unresolved-import), so gating now would fail the tree; the cleanup is scoped for after the in-flight branches merge, and spicy-regs gates it in CI and pre-commit: clean at 1f18898 under the default install, the earlier `test_fec_relationships` diagnostic fixed on the linkages branch, and two latent diagnostics in `vectordb/embed.py` that surface only with the `embed` extra, which the gate never installs. |
| Releases | spicy-docs 0.21.2 cut (commit 3642aa1, tag v0.21.2, wheel sha256 `96eb4897…`) and adopted on spicy-regs `billtrax-hosting-prep` (merge f198b6e, reviewed): the interim MODS bill parse is deleted for `PackageModsIdentity.primary_bill`, the ten new contracts are an explicit unhosted set the registration test checks exactly, and the two hosted tables whose installed prose changed carry data-quality clauses. The rollup wave for those ten landed (merges a431fef and fdc3d8e) and D1 measured every rollup (merge bc82c51). spicy-docs 0.21.3 cut (commit 083b536, tag v0.21.3) with the prompt-shape fix and adopted on the hosting branch (review in progress), then 0.22.0 cut (tag v0.22.0, wheel sha256 `782735d8…`) with the request schema, the refusal guard and the v3 classification prompt; spicy-regs adopted 0.22.0 on the hosting branch (merge 2d13f81, reviewed twice): its own adapter and refusal wrapper deleted for the ones spicy-docs ships, the first tests that run the model readers end to end, refusal reasons reaching the run log. The remaining step is a keyed production run, which now publishes summaries and classifications instead of refusing every answer. spicy-docs 0.23.0 cut (commit 73b10e5, tag v0.23.0, wheel sha256 `36d619a6…`): the five PDF-family contracts, their two rule modules, the widened grammar and the Zyte transport; spicy-regs adoption built 2026-09-20 on the fork worktree branch `adopt-spicy-docs-0.23.0` (five commits over 2d13f81; 1,680 tests, ruff and ty clean, 64 dictionary tables); **landed 2026-09-20 (merge bdedd8f on `billtrax-hosting-prep`, pushed to the fork)** after review and a fix round with mutation-checked tests; the Senate re-run read the fifth report (649 rows, 12 requests), so `senate_expenditures` holds 3,272 rows over all five reports and the worst hour is 271 keyed. The review's findings, all fixed: the Senate rollup's per-run cap counted already-held packages, so the fifth report (`GPO-CDOC-118sdoc11`) was never reached while its dictionary page said it would be; the print-citations cold start spent its cap on CRPT and published an empty `budget_volumes` until the resume; the host's PDF-first preference was a hand copy of the sealed order bound to no test. Everything re-derived held: wheel bytes and the 37-over-766 registry verified from the wheel itself; two rollups, `print-citations` (house_activity_reports, budget_volumes, bill_committee_actions, document_citations) and `senate-expenditures`; one measured run each under caps written before the run (receipt `rollups-pdf-families-2026-09-20/`): 2,623 Senate rows, 41 reports, 23 budget volumes, 12,700 actions, 49,792 citations, worst rolling hour 261 keyed against the 4,000 ceiling; the resume skipped 40 published packages at zero request cost. The run exposed three spicy-docs follow-ups (filed for the next release): the HTML rendition refused 10 of 41 activity reports on nesting depth and states no page, so the print families read PDF-first behind the sealed order, a per-family preference the package should own; a 2023-01-01 BUDGET walk serves 17 real packages whose part is outside the sealed six (`OBJCLASS`, `TAB`, `DB`, `CLIMATE`, `LRB`, `CROSSCUT`, `DOD`), refused by name until the grammar widens on that measurement; and the activity-report title rule lives in `tools/`, so the host restates it. Corrected at five times the sample: over 41 reports the print states 7 bills of 7,686, 5 laws of 990 and 6 Code sections its index does not. spicy-docs 0.24.0 cut (commit 713c821, tag v0.24.0, wheel sha256 `0a03ce34…`): `hearing_bill_links`, `cbo_cost_estimates`, the `house_communications` backfill columns, the CBO letter columns on `committee_reports`, the per-bill estimate outcome on `congress_bills`, the Record communications and repository readers, the moved title rule, `PRINT_BODY_PREFERENCE`, thirteen budget parts, the Mirrulations recovery fix; spicy-regs adoption in progress on a Codex worker. spicy-docs 0.24.1 cut (commit fc8171d, tag v0.24.1, wheel sha256 `ce25270b…`): the Mirrulations record-identity check, no contract change; the host drops its temporary guard on adopting it. **spicy-regs adopted 0.24.1 and landed it** (merge ad4c097 on `billtrax-hosting-prep`, pushed to the fork; 1,801 tests, ruff, ty and the 67-table dictionary clean, generation idempotent over 69 artifacts): hearing links and CBO estimates hosted through their owning rollups, the letter columns and the communication provenance columns filled, three restatements deleted for the package's own rules, and one measured run of each changed rollup (236 requests, 177 keyed, worst hour 177 of 4,000). Reviewed with four defects fixed: an empty envelope and a publisher error document became coverage, a broad handler swallowed credential refusals in the printing path, unresolved-key history did not survive a fresh runner, and additive merges could not retire a relationship a re-read parent had dropped. spicy-docs 0.24.2 cut (commit 965776e, tag v0.24.2, wheel sha256 `cf84fb9f…`): the retry's scrub-before-log order and the S3 stream close, both from the duplication audit's correctness counterexamples; no contract change, so a host adopts it by bumping the pin. **Adopted on spicy-regs the same day** (commit 2b7b5e4 on `billtrax-hosting-prep`, pushed to the fork): a pin bump only, the registry re-imported from the vendored wheel at the same 39 contracts over 813 columns, 1,801 tests with ruff, ty and the 67-table dictionary clean and generation idempotent. Both fixes land where this host needs them: every keyed rollup retries, and the comment ingestion reads the objects whose bodies now close. |

### A. Joins the data map measured and nothing drives

The [legislative data map](legislative-data-map-2026-09-18.md) resolved 51
of 55 edges on real items. These are the ones with no consumer.

| # | Gap | Evidence | Fix | Proof | Home |
|---|---|---|---|---|---|
| A1 | `press_releases.bill_id` is NULL though `release_matching` exists | spicy-regs `build_press_releases.py:15` | The transform reads the current `congress_bills` identities (one prior download it already makes) and calls `match_releases` with one compiled pattern per bill | Fixture run: the House feed's items that name a bill match; `matched_field` is `title` on the Senate feed | spicy-regs |
| A2 | `committee_reports.bill_id` and `hearing_transcripts.bill_id` are NULL | `build_committee_reports.py:18`; the map's `report→bill` edge resolved 17 of 17 via `associatedBill[]`, `hearing→meeting→bill` 6 of 6 | The committee-report and hearing list routes (already in `listing.py`) supply the linkage; the transform indexes packages by the stem the route's format URL states and fills `bill_id` from `associatedBill` and from `relatedItems.bills` | Fixture pages from the two routes; every CRPT row for the 119th sample gets a bill or a named refusal. **Hearing half closed differently 2026-09-20**: `hearing_transcripts.bill_id` is NULL by design and `hearing_bill_links` hosts the one-to-many relationship instead | spicy-regs |
| A3 | `recordedVotes` on bill actions is not a second vote linkage | `build_roll_call_votes.py:17-21`; the map resolved `bill→house-vote` 5 of 5 and `bill→senate-xml` by url | The family rollup already holds every bill's actions; it emits the `(bill_id, chamber, congress, session, roll)` references as a fifteenth family output, and the votes rollup joins on them at merge time, so no rollup reads another's output | The 58-entry measured sample resolves 34 distinct roll calls to bills | spicy-regs |
| A4 | Senate roll calls have no index route | Congress.gov has `house-vote` only; the Senate LIS `vote_menu_{c}_{s}.xml` serves the session's list (measured: 101st onward, 149 KB) | `sources/congress/votes.py` gains `list_senate_votes(congress, session)` over the menu file, keyless, identity proved from its own congress and session; the votes rollup walks it newest first with the same held-set skip | Fixture menu; the first vote of the 119th resolves to `vote_119_1_00001.xml` already fixtured | spicy-docs, then spicy-regs |
| A5 | The regulatory bridge is acquired and unconsumed | House communications carry `isRulemaking`, CRA authority, committee referral with system code, matching requirement and the RIN in `reportNature` (17 of 25 sampled are rulemakings); the RIN resolves in the Federal Register API | **spicy-docs side landed 2026-09-19**: the `house_communications` contract in `schemas/congress_index_tables.py` (27 columns: every typed detail field, the referral's system code and date, `is_rulemaking`, the cited `legal_authority`, the matching requirement, and `rin`/`rin_rule`/`rin_matched_text` from the measured `RIN: nnnn-XXnn` rule in `interpretation/communication_rin.py`). The Federal Register side needs no acquisition change: the source record already carries `regulation_id_numbers` and `schemas/federal_register.py` publishes `regulation_id_numbers_json`, so the `rin` join is spicy-regs rollup work. No `senate_communications`: the Senate detail carries none of the bridge's fields (module docstring). Still open: the spicy-regs rollup and join | Fixture EC 4752 with its list row through the generic loop; re-measured on 18 of the 25 newest inside the day's 20-request budget: 18 of 18 carry a dated referral, 12 are rulemakings and the same 12 carry a RIN under the measured rule and under a relaxed FR-shaped rule, 15 name a requirement (`corpora/supply-2026-09-02/receipts/house-communications-rin-2026-09-19/`; `tests/test_congress_index_tables.py`) | spicy-docs contract, spicy-regs rollup |
| A6 | The requirement route is a count, not an index | Requirement 8070 lists 92,450 communications unordered by Congress; a full walk is 370 pages, about five minutes; detail records exist only from the 114th | Run the two bounded measurements the map names: walk the list once and histogram by Congress; probe one detail per Congress from the 105th to pin the floor. Host `house_requirements` (3,226 rows, a 2021 snapshot) only if the histogram shows the detail era covers a useful share | The histogram and the floor, written into the map | spicy-docs measurement first |
| A7 | Meetings, hearings and documents chain unhosted | `committee-meeting` (111th+, 18,133) links `eventId → hearing jacket → CHRG package` and `relatedItems.bills`; `witnessDocuments[].url` resolve | **spicy-docs side landed 2026-09-19**: the `committee_meetings` contract keyed `(congress, chamber, event_id)` with `hearing_jacket` beside `hearing_jackets_json` (the captured hearing's meeting names two jackets), `bill_ids_json`, `document_urls_json` and every typed detail field; `hearing_transcripts` gained `event_id`, appended last; a `hearing-detail` route in `listing.py` supplies it from `associatedMeeting.eventId`. Still open: the spicy-regs rollup | `hearing→meeting` and `meeting→hearing` re-run on the captured pair (jacket 64431 ↔ event 119003, each way), `meeting→bill` 9 of 9 on the captured markup 119565, `record→package` by the hearing's own `formats[].url` stem, all in `tests/test_congress_index_tables.py`; the pair cost two keyed requests (`corpora/supply-2026-09-02/receipts/committee-meetings-edges-2026-09-19/`). The map's 4 of 4 and 6 of 6 were live samples; the fixtures hold one pair and one markup. **2026-09-20**: `event_id` now reaches docs.house.gov's agenda through a keyless GET, behind a per-row committee-and-date identity check (`tests/test_hearing_bill_links.py`) | spicy-docs contract, spicy-regs rollup |
| A8 | Enacted-law list, Statutes at Large, OLRC tables unused | `law/{c}` is the cheap enumeration (108 rows for the 119th); PLAW USLM states `NNN Stat. NNN`; STATUTE bulk reaches volume 137; Table III and the classification tables resolve from the law number | **Landed 2026-09-19:** `laws`, `law_code_sections` and `table3_records` in `schemas/law_tables.py`; the OLRC per-Congress table reader in `sources/uscode/classification.py`; `law_type` sealed `public`/`private` (decisions.md). `statutes_at_large_cite` stays NULL in the family build by the one-pass rule and is published on `laws` (`uslm_outcome` says why when NULL); the host joins on `bill_id` at merge | Rerun 2026-09-19: 108 laws on the route, 104 in PLAW bulk, 4 lagging (119-103, -104, -109, -110); the 2nd-session table parsed 583 rows in both orders, same multiset, 38 stated laws; Table III page states act, Congress and volume back (`corpora/supply-2026-09-02/receipts/laws-contract-2026-09-19/`; `tests/test_law_tables.py`) | spicy-docs contract, spicy-regs rollup |
| A9 | Rosters: committees and members from the API, assignments from the chamber files | `committee` (818, from the 53rd) and `member` (2,696, from the 68th) are API-first; the map's comparison found the chamber files add only committee assignments and the LIS id | **Landed 2026-09-19:** `committees` and `committee_assignments` in `schemas/roster_tables.py`; the chamber-file readers in `sources/congress/committee_rosters.py`, identity proved from each file's own statement (House states Congress and session; Senate states none, and `congress_basis` publishes the caller's); the legislators JSON stays the LIS crosswalk (decisions.md) | Rerun 2026-09-19: API 555 vs files 541 for the 119th, all 14 API-only with ended terms; the committee route served 236 of 238 declared and the walk refused it, 183 rows with parent, 31 with subcommittees (`corpora/supply-2026-09-02/receipts/roster-comparison-2026-09-19/`; `tests/test_committee_rosters.py`) | spicy-docs sources and contracts, spicy-regs rollup |
| A10 | Treaties, committee prints, the daily Record index, nominations feeds | `treaty` (786) resolves to CDOC; CPRT (7,784) is not in the body grammar; `daily-congressional-record` is the legislative-day calendar (sections per issue name the chamber); nomination feeds are a keyless status cross-check | **Contracts landed 2026-09-19** in `schemas/congress_index_tables.py`: `record_issues` keyed `(volume, issue)`, its `chambers` derived from the detail's section names as the calendar and its `package_id` from the whole-issue link's stem; `treaties` keyed `(congress_received, number, suffix)` with `package_id` by the `CDOC-{c}tdoc{n}` rule on an unpartitioned treaty; `nominations` keyed `(congress, citation)` over the list route. Feeds stay a cross-check with no contract. The CPRT row landed on main with the granule and USLM work (merge 077091a). Still open: the spicy-regs rollups | `record→legislative-day` on issue 172/148 (a Senate-only day) and `record→package` (`CREC-2026-09-18`), `treaty→cdoc` on 119-2 → `CDOC-119tdoc2`, on fixtures in `tests/test_congress_index_tables.py`; every contract through the generic loop from captured list pages and details | spicy-docs, spicy-regs |
| A11 | Bills before the 108th have no bulk status | BILLSTATUS bulk is 108–119; the API bill list reaches the 82nd | The family rollup's backfill for older Congresses walks the `bill` list route by Congress under the same per-run cap and builds status from the detail route; the contract is unchanged | One older Congress walked end to end, counts against the route's declared total | spicy-regs |
| A12 | The map document is stale about what landed | Rows for bill PDFs, press releases, report parsing, both vote XML files and the two matching rows still read `port` or `candidate` or `rejected (here)` | Update `ROWS` in `tools/analysis/legislative_data_map.py` to `have` with the module paths; regenerate with `--offline`; the fixture test then proves every `have` row names a file that exists | `uv run --frozen pytest -q tests/test_legislative_data_map_tool.py` | spicy-docs |
| A13 | Floors the walk stopped short of | Committee prints populated again at the 94th, treaties at the 81st, committees below the 60th cap | Raise the walk caps for those three routes and re-run `--floors` | The regenerated floors | spicy-docs |

### B. Bodies and extraction

| # | Gap | Evidence | Fix | Proof | Home |
|---|---|---|---|---|---|
| B1 | Bills before the 113th have no XML, so no section tree and no diff (sized by the [HTML-to-XML gap measurement](bill-html-xml-gap-2026-09-19.md), 2026-09-19) | [Census](pdf-only-corpus-2026-09-19.md): 2005-era and 1990s-era bills offer `htm` and `pdf` only; the BILLS API reaches 1993 | Reconstruction (§3): the HTML rendition into the bill DTD that DeltaTrack parses, so ten Congresses gain sections and diffs | The paired benchmark: hide the XML of 113th-era bills, reconstruct from their HTML, compare | spicy-docs |
| B2 | The daily Record is PDF-only at package level | CREC packages state `pdf` only; the granules have HTML | Body acquisition takes a granule id (the grammar already accepts the split-day suffix) and prefers the granule's HTML; the package PDF stays the fallback | One issue's granules fetched and parsed; the package PDF path exercised once | spicy-docs |
| B3 | CRS HTML answers 403 to a plain client while the API states it | Measured live 2026-09-19 | Measure once whether the publisher's stated `formats[]` HTML answers with the same headers the CRS file route already uses; if it does, HTML becomes the preferred rendition for CRS; if not, record the refusal as the publisher's and keep the PDF | The measurement, in `docs/sources/crs-files.md` | spicy-docs |
| B4 | ~~CBO estimates sit behind a bot wall; the Court, disclosure sites and activity reports are PDF-only with no target vocabulary~~ **Superseded 2026-09-20 — see the §2.0 row above.** The gap was reopened with a target, then re-scoped: the activity reports' keys are already in their own package MODS | ~~Census; the map's `rejected` rows~~ [`pdf-family-rollup-yield-2026-09-20.md`](pdf-family-rollup-yield-2026-09-20.md), corrected by [`pdf-yield-mods-recheck-2026-09-20.md`](pdf-yield-mods-recheck-2026-09-20.md) | ~~No change: extraction into the evidence model only; no schema target exists~~ `document_citations` on the budget volumes; a MODS loader for the CRPT-stated keys; the narrowed activity-report print contract | The recheck's sidecar and its 24-request receipt | spicy-docs |
| B5 | Table geometry and reading order are not retained | `extraction/model.py` keeps `raw` (PyMuPDF's full dict) but no table structure; the CRPT measurement showed PyMuPDF emits every label then every amount | Add a `TableObservation` to the evidence model built from PyMuPDF's `find_tables` on the retained page, kept beside the text, never merged into it; measure against the HTML's 841 intact rows on CRPT-113srpt77 | Row-recovery rate on the two appropriations reports whose HTML is the reference | spicy-docs |
| B6 | The GPO normalizer is validated on four fixtures and synthetic cases | The below-floor run test's residual false-positive shape is stated, not measured | A corpus run: forty GPO bill PDFs across print stages and Congresses through the integration path, with the layout verdict and rejoin counts pinned per document, the way upstream validated its floor on sixty | The pinned table in `docs/extraction-gpo.md` | spicy-docs |
| B7 | The USLM bill rendition is recognized but not acquirable | `PACKAGE_BODY_FORMATS` is htm, xml, txt, pdf; the sample's 240-entry format aggregate double-counts one bill (hconres11, pinned by two codes) -- 9 distinct USLM entries, 5 of them BILLS packages (4 are PLAW-collection entries outside the BILLS grammar) | Add the `uslm` rendition (folder `uslm/{id}.xml`) to the grammar and the sealed order after `xml` (it is a second structured rendition); measure on every BILLS package in the sample that offers one | The five fetched and parsed through the body acquirer -- `markup-reader`, per the decision record (`uslm.py`'s grammar is PLAW/COMPS-scoped) -- pinned per package in `tests/fixtures/govinfo_bills/uslm-renditions-2026-09-19.json` | spicy-docs |
| B8 | Extraction backends beyond native text are unqualified for this corpus | `docs/extraction/pdf-extraction-choices.md`: OCR and vision candidates measured on forms, not on legislative print | Scope OCR to the one corpus that needs it, pre-XML scans (CFR and Federal Register editions before their XML era), and only when a consumer asks; the born-digital program needs none | Deferred until a corpus asks | spicy-docs |

### C. Interpretation

| # | Gap | Evidence | Fix | Proof | Home |
|---|---|---|---|---|---|
| C1 | The model-backed modules have never run live | `classify_sections`, `summarize_bill`, `summarize_diff` are wired behind `GEMINI_API_KEY` and exercised only with stubs | One bounded live run over one fixture bill through `extraction/gemini`'s client adapter, recording model, prompt digest, tokens and cost; the coverage statements then say "measured" | The run's provenance rows in the model tables; cost per bill stated | spicy-regs run, spicy-docs adapter (moved in-repo 2026-09-20: `interpretation/gemini_call.py`) |
| ~~C1a~~ **closed** | `classify_sections` published no row: the model returned the prompt's brackets | Measured twice live, 2026-09-19 and 2026-09-20 (`c1-classification-2026-09-20/`): `[introduced-in-house\|govinfo\|0]` for `introduced-in-house\|govinfo\|0`. `section_block` writes `[<id>] <heading>`, BillTrax's own line; the `v2` `CLASSIFICATION_FIELDS` asked for "the section's bracketed id, copied exactly as given below"; `_read_row` requires the bare id | Done: the `sectionId` field asks for "the text inside the square brackets below, without the brackets", `PROMPT_VERSION` is `v3`, its digest re-pinned (`63005e50…` over the test's own sections). `section_block`, `_read_row` and the `{"type": "string"}` schema for `sectionId` all unchanged — an `enum` of the batch's ids would also produce rows and is the wrong fix alone, because it makes a badly worded prompt look correct | **Two** keyed calls over the same fixture batch (`c1-classification-v3-2026-09-20/`, prompt `411e9f67…`, 534 in / 200 out, USD 0.00066): both answered in the batch's own ids, both stored three rows under `prompt_version` `v3`, no refusals, no substituted id. The `v2` answer is a committed fixture whose test still shows it refused | spicy-docs |
| C2 | `interest_areas` cannot reproduce MySQL boolean-mode search | `docs/interpretation.md`: the token-length floor, the stopword list and the relevance order are unreproduced | Measure BillTrax's engine defaults (InnoDB minimum token length 3, its stopword list, its relevance formula) against the pure implementation on the fixtures; either encode them as a named rule or state the divergence on the finding | A parametrized test over the measured cases | spicy-docs |
| C3 | The signed-date fallback rule's frequency is unmeasured | `public_law_without_became_law_action` exists so a fallback need can be counted | Count it over one Congress's enacted bills from bulk status | The count, in the module docstring | spicy-docs |
| C4 | The bill-signals audit harness has no run | `tools/analysis/audit_bill_identify.py` reads a candidates CSV nobody has produced | Export the candidates from one family run and run the tally; compare with BillTrax's reported rates | The tally in the research folder | spicy-docs |
| C5 | Report-block headers on all-caps title-page sentences | Documented limit on the parser page | Leave as is; the HTML rendition removed the hyphen-wrap fragments, which were the measured defect | None | none |

### D. Hosting

| # | Gap | Evidence | Fix | Proof | Home |
|---|---|---|---|---|---|
| D1 | Coverage statements say "Sampled, and accumulating" with no full run measured | Every rollup is stubbed in tests; no production run has happened | One measured run per rollup on the fork branch against real publishers, under the stated caps, recording rows, requests, wall time and refusals; `measured_on` set from it | The dictionary's coverage statements carry the numbers | spicy-regs |
| D2 | SR01: spicy-regs's hand-rolled `congress_bills` reader still exists beside `listing.py` | spicy-regs `PLAN.md`; the narrow writer keeps the frozen ten-column prefix | Retire the reader in favour of the bill list route with the same window logic; the coalesce merge already protects the family's columns | The 21 existing tests plus the end-to-end merge test stay green | spicy-regs |
| D3 | spicysearch vendors a 24-table catalog | Measured: `01c77a4a…` vs the fork's `d08822d8…`; 47 tables on the hosting branch, 49 once A11 merges | Re-vendor after the spicy-regs branch merges upstream | spicysearch's receiver tests | user, then spicysearch |
| D4 | `hearing_transcripts` is exercised only by a synthetic record | Contract tests: the loop runs the captured CRPT-119hrpt1 body under the real CHRG-119hhrg64431 identity, never a CHRG body | Capture one small CHRG package's HTML body as a fixture with provenance | The generic contract loop runs on it | spicy-docs |
| D5 | The family rollup re-walks eight archives per run | The listing skip removes the download, not the walk; each unchanged archive still costs one listing read | Acceptable: eight small reads. Measure a night's request count in D1 and stop here unless it exceeds the cap | D1's numbers | none unless measured |

### E. Upstream and tooling

| # | Gap | Evidence | Fix | Proof | Home |
|---|---|---|---|---|---|
| E1 | Seven validated DeltaTrack gaps are unfiled | [Issue list](deltatrack-upstream-issues-2026-09-19.md): A1 collision-group cap (8.74 s for 300 same-path sections), A4 hyphen-tolerant tokens, A6+A7 version keyword and a parsed-tree entry, A8 the XML path imports pypdfium2, B1 the DSK-literal regex, B4 and B5 the two hyphen mechanisms | File them as same-org issues in priority order with the bodies as written; A6+A7 also removes the temp file and second parse in `bill_tree.py` | Issue numbers recorded in the doc; the pin moves when a fix lands | user files; spicy-docs tracks |
| E2 | DeltaTrack is pinned by git sha; the gate fetches GitHub on a cold cache; spicy-regs vendors a locally built wheel | `pyproject.toml`; `docs/sources/congress-bill-tree.md` | Ask upstream for a tagged release and a PyPI publish (one more issue); move both pins to it | The pin line changes once | user, upstream |
| E3 | `ty check` is configured but not gated in spicy-docs; spicy-regs gates it and is clean under the default install (its one earlier diagnostic was fixed; two latent ones need the `embed` extra) | `scripts/check` runs ruff and pytest only | Add `uv run --frozen ty check` to `scripts/check` once clean; fix the spicy-regs diagnostic | The gate | spicy-docs, spicy-regs |
| E4 | Integration tests never run on a schedule, so publisher drift is found by accident | A pinned Federal Register day's replay now fails on a digest mismatch, found while doing other work | A weekly workflow running `-m integration` against the live publishers and filing the drift as a report, never a failure of the default gate; triage the FR day now | The first weekly report | spicy-docs |
| E5 | The docs index is maintained by hand | Five index lines added today by hand | A test that every `docs/sources/*.md` and top-level guide is linked from `docs/README.md` | The test | spicy-docs |

### 2.6 Candidates from the 2026-09-20 validation (filed, unmeasured)

The outside read of the register found nine gaps no row names. Each is a
candidate until its measurement runs; none is a commitment. F2 is measured and
closed; the other eight remain candidates. Review added F2b to track adoption
of the shared Mirrulations reader in spicy-regs.

The [duplication audit](dry-audit-2026-09-20.md#correctness-counterexamples-without-requests)
adds F10 and F11 with offline counterexamples. SD1 in §2.0 now records their
implemented and measured remedies; the original audit and proposed checks
below remain historical evidence. Public adoption is tracked separately.

| # | Candidate | Evidence | Why it matters | Fix | Proof | Home |
|---|---|---|---|---|---|---|
| F1 | Regulatory joins incomplete | spicy-regs `build_federal_register.py`: 696,679 legacy rows lack RIN arrays; the scalar column keeps only the first RIN | Historical joins missing; second and later RINs vanish from scalar joins | Backfill the arrays and expose a document-to-RIN relation | Count recovered legacy arrays; compare joins against every array element, including the 1,499 multi-RIN documents | spicy-regs |
| F2 | Mirrulations recovery breaks the fetcher rules | `sources/mirrulations.py`: all download failures wrapped, `fail_fast=False` by default, parse failures stay processed; a test requires it | A credential refusal can become a skipped row; repaired records stay excluded on resume | **Closed 2026-09-20**, the first candidate measured rather than filed. The validation's three readings all held; a fourth defect it did not name was worse: a 2xx body of `{}`, `null` or a bare scalar was **yielded as a record and manifested**, so an empty answer became apparent coverage. Now: a 401/403 during an agency-object listing or GET raises `MirrulationsAccessRefusedError` (a `CredentialRefusedError`) through a shared check, writing no key and unreachable by `fail_fast`; every key that produced no record stays off `last_keys` with a `KeyOutcome(key, status, reason, attempted_at, attempts)` on `unresolved`, status `transport`/`unreadable`/`requested-empty`, all three retried next run and, via `unresolved_keys=`, ahead of newly listed work; passing prior outcomes carries attempt counts forward, with no automatic retirement. Reasons are scrubbed before truncation, and `scrub_credential` grew the three AWS presigning parameters beside `api_key` — the new test found that the added alternation had been swallowing the `=`, so the marker read `X-Amz-Signature<redacted>`. `DownloadFailures` is gone for `list[KeyOutcome]`; `parse_failed_keys` survives as the non-transport subset but is no longer manifested ([decisions](../decisions.md#a-mirrulations-key-that-produced-no-record-is-unresolved-never-processed)). Follow-up from the downstream review: the shape check now requires record identity, a nonblank `data.id` for dockets, documents, and comments. The reproduced `{"data":{}}` and `{"errors":[{"detail":"upstream failed"}]}` formerly yielded null-id records and manifested their keys; both now stay `requested-empty`, with the missing identity named and publisher errors scrubbed before truncation. The host carries a temporary guard until it adopts the release containing this fix | Proved offline: 401 and 403 injected on both download branches, all three agency-object listing paths (including lazy pagination), and the exact enumeration abort with the typed refusal; `fail_fast=True` also aborts. Repeated unresolved outcomes retain cumulative attempt counts; a 503-then-success and a malformed-then-repaired object recover across runs; five empty/mis-shaped bodies record `requested-empty` with the shape named. In the original regression set, each load-bearing mutation fails seven tests: removing the abort fails four raw-reader refusal cases, both exact-enumeration refusal cases and the no-retry refusal test; restoring parse failures to `last_keys` fails the repair test, the five `requested-empty` cases and the rewritten parse-failure test. Each scrub pass still fails a test of its own. Original `./scripts/check`: 6,691 passed | spicy-docs |
| F2b | Independent Mirrulations reader retains the original defects | Review identified a 524-line copy at `src/spicy_regs/sources/mirrulations.py`, run by `pipelines/regulations.py:129`: it wraps all download failures, defaults to `fail_fast=False`, and marks parse failures processed | The spicy-docs fix does not reach that pipeline while the copy remains | At the next spicy-docs adoption, delete the copy and import `spicy_docs.sources.mirrulations`; do not patch the duplicate | The copy is removed, the pipeline uses the shared reader, and adoption checks prove typed refusals and recovery across runs | spicy-regs **Closed 2026-09-20** (spicy-regs merge ad4c097): the copy is deleted for `spicy_docs.sources.mirrulations`, the pipeline resumes unresolved keys through the shared reader's own state, and the guard the host added meanwhile is retired because 0.24.1 requires the same nonblank data id for dockets, documents and comments and rejects an error envelope besides. |
| F3 | Amendment bodies missing | the map's amendment rows; the contract is metadata-only | Amendments can be identified, not read or compared | Measure the stated text routes; retain renditions; link bodies to amendment ids | Stratified amendments through acquisition, identity and hosted joins, refusals reported | spicy-docs, then spicy-regs |
| F4 | Model usefulness unqualified | `docs/interpretation.md`: live diff and classification quality unmeasured | Valid JSON can still be a wrong classification or a misleading summary | Independently judged real-bill and changed-version samples | Per-label accuracy and abstention; source-supported summary claims on held-out documents | spicy-docs; spicy-regs run |
| F5 | Historical law bodies absent | the map distinguishes PLAW bulk from unimplemented STATUTE XML | Older law metadata has no structured text behind it | Bounded STATUTE acquisition; section-to-law identity mapping | Early volumes reconciled to law identities with retained text | spicy-docs, then spicy-regs |
| F6 | Senate expenditure detail incomplete | `docs/tables.md`: payee and payment fields unparsed; compensation and mail sections unqualified | Ruled cells cannot answer payment-level questions or prove whole-report coverage | Qualify the later sections; measure payment-block interpretation | Independently labelled payments; attribution accuracy; reconciliation per grid | spicy-docs, then spicy-regs |
| F7 | House spending absent | the map: the House disbursement CSV is a candidate; USAspending excludes Congress | The Senate side is covered and the House side is not | Acquire the publisher CSV with period and file provenance; define rows | Full-period counts and totals reconciled to the published statement | spicy-docs, then spicy-regs |
| F8 | CRS run provenance incomplete | `crs_summaries.py` rows keep a source path but no run id, capture time or input digest | Appended results cannot be attributed when inputs change in place | Immutable run and input pins; per-attempt observation metadata | Resume across two input versions and reconstruct every row's origin | spicy-docs |
| F9 | Older vote corrections undetectable | spicy-regs `build_roll_call_votes.py`: no publisher update field; only the newest overlap rechecked | Corrections outside the overlap persist | Retain update signals where supplied; bounded historical refresh | Change a held older vote; prove detection and corrected rows | spicy-regs; shared schema if needed |
| F10 | **Specification corrected by SD1:** a digest version establishes equality, never chronological ordering | `schemas/tables.py:114` says a merge prefers the larger version, and two contracts put a rule digest there, which a correction can move downward — **but the host's merge orders `_src DESC` before the version** (`table_merge.py:171`), so a fresh row already outranks a prior one and the defect bites only between two rows of the same source | Specification debt rather than a live loss: the published rule is wrong and a second host implementing it literally would keep a superseded row | State that a digest version column is compared for equality and never ordered, or add a monotone revision column beside it and keep the digest as provenance | Two same-key rows whose chronological and lexical orders disagree, merged correctly under the stated rule | spicy-docs, host informed |
| F11 | **Closed by SD1:** shared JSON encoder now refuses non-finite values with named row outcomes | `schemas/tables.py:83` emits `{"value":NaN}` despite its refusal promise; `bill_diff_tables.py:225` passes arbitrary engine evidence through `shape_section_diff_item`. No committed fixture was found with a non-finite value. Neighbouring `schemas/document_capture/xml.py:90` already refuses these values | Admitted input can publish invalid JSON in evidence columns; this is input risk, not measured corruption | Audit every `json_column` caller and its input/refusal handling first, then decide on the stdlib `json.dumps(..., allow_nan=False)` switch. Preserve finite-value bytes; no custom serializer | Exercise nested NaN and both infinities through `json_column` and the section-diff shaper; require refusal. Compare exact output bytes for finite fixtures across audited callers | spicy-docs; inform the spicy-regs host before adoption |

The validation also found four kinds of structural drift the rows above now
correct or disclose: the header's completion summary, header snapshot numbers,
"hosted" used for contracts not yet adopted downstream, and a closure (C5)
filed against a different defect than the row names. A12's offline refresh
corrects the stale map rows and separates the unsupported CRECB body claim;
its local commit and checks are recorded in §2.0.

### 2.8 Output observations and corrections (updated 2026-09-21)

The original sample read 62 local files: 61 declared tables and one undeclared
`bill_subjects` output. It selected retained runs and older experiment files;
it did not inspect production. The 2026-09-21 public observation and native
BILLSTATUS/PDF replays correct those conclusions below. Evidence and exact
scope are in [the current note](legislative-readiness-2026-09-21.md).

| # | Irregularity | Evidence | Why it matters | Fix | Proof | Home |
|---|---|---|---|---|---|---|
| H1 | All-text storage is a consumer cost, not an undeclared irregularity | 1,091 of 1,094 sampled columns are parquet `string`; **but `docs/tables.md` declares it in its second paragraph** — every value is a string or NULL, published as all-VARCHAR parquet read through a DuckDB view, so a typed value is spelled once in `schemas/tables.py` | The storage is deliberate and stated; the cost is that every consumer casts, and a bad cast is silent | **Not a migration.** Add validation that each column parses as its declared logical type, and a typed consumer view where one is wanted | A reader over the published parquet asserting each column's logical type | spicy-regs |
| H2 | Seven sampled local tables have schemas and no rows | The original local scan found empty `bill_summaries`, `diff_summaries`, `financial_changes`, `section_classifications`, `bill_family_backfills`, `bill_family_backfill_walks`, and `hearing_bill_links`; their public URLs returned 404 on 2026-09-21 | Local emptiness and public absence are separate observations, neither proves publisher absence | State input scope, processing outcome and publication separately | Per-table retained output and publication observation | spicy-regs |
| H3 | **Original conclusion refuted:** six locally missing tables are publicly available | Live footer reads on 2026-09-21 found nonempty `agency_stats`, `agency_monthly_volume`, `discovery_signals`, `feed_summary`, `rulemaking_lifecycles`, and `org_committee_links` | A local file inventory cannot establish public availability | Keep these six tables; retain direct artifact observations | All six public Parquet footers retained with HTTP/ETag evidence in `publication-audit-2026-09-21` | spicy-regs; no withdrawal |
| H4 | Broader local runs and public bills have different scopes and shapes | Historical local runs are 419,621 rows × 48 columns and 16,213 × 49; public observation is 419,571 × 10. New corrected local candidate is 16,213 × 49 for 118th HR/S | These are distinct artifacts, not contradictory measurements of one generation | Keep the validated 118th HR/S candidate separate; qualify and correct wider retained data before a broad replacement | Five-table candidate passes 1,189,950 named native-field comparisons; wider release remains open | spicy-regs, with spicy-docs parser fix |
| H5 | `bill_subjects` has an undeclared local output; public publication is not established | Local rollup/workflow and 20,013-row file exist; public URL returned 404 on 2026-09-21 | Prior wording incorrectly promoted a local file to published data | Declare and validate it before future publication, or explicitly keep it local | Public observation retained; no current published extra table claimed | spicy-regs |
| H6 | **Corrected locally, not deployed:** changed rules now invalidate successful reads | Print and Senate pipelines formerly skipped on publisher timestamps or file presence alone | Corrected code otherwise leaves old findings unchanged | Store successful-read checkpoints with output metadata; require relevant rule/source agreement; replace re-evaluated findings including empty results; preserve failed reads and print text-digest history | Regression cases and original PDF/output replays in `correction-lifecycle-2026-09-21`; independent review and gates in the current note | spicy-regs |
| H7 | **Mechanism fixed and live-R2 tested; wider adoption open:** complete Parquet families publish through one conditional generation pointer | The original `pipelines/rollups/base.py:51` permitted partial publication of a family's outputs | An interruption between uploads leaves a reader on a mixed generation, some tables new and some old, with nothing saying so | Publish a family's artifacts through one generation reference so a reader either sees the whole new generation or the whole previous one | An interrupted publish that leaves every reader on the previous complete generation | spicy-regs |
| H8 | Schema declarations and `measured_on` do not establish publication | Direct 67-URL audit found 24 public files, 43 HTTP 404s, and four public schemas different from the local declaration | Consumers need distinct declaration, local-run and publication states | Local dictionary wording now states support, generated pages state that availability is unverified, and a separate per-artifact observation records status/ETag/footer/shape/counts | `publication-audit-2026-09-21` retains raw footer evidence; complete-family admission is now implemented and live-R2 tested (SR1 and Cloudflare rows in §2.0); real-source rollout and other publication paths remain open | spicy-regs |
| H9 | **Prose corrected locally; qualification remains open:** single-bill action rows have measured precision, not a trust guarantee | 30 of 36 hand-checked single-bill findings were correct (83.3%); the filter excludes multi-bill sentences | Filtering alone does not certify the remaining findings | Provider descriptions and guide state the measured rate and require a separate acceptance decision; no threshold invented | Text no longer calls the class trusted or hosted-quality; no new semantic-quality measurement claimed | spicy-docs; downstream prose adoption pending |
| H10 | **Fixed and installed by the host; wider replay/publication pending:** cosponsor counts used the wrong XML list | Direct census of 16,213 native 118th HR/S documents found 13,154 incorrect zero counts; 162,424 entries occur in the separate cosponsors list | A schema-valid output can still misstate a publisher fact | Parse the separate list and count its items, including withdrawn entries; preserve NULL for an unexamined caller-created status | Real 49-cosponsor fixture reproduces failure; corrected five-table rebuild passes 1,189,950 named source-field comparisons and changes only 13,154 count cells | spicy-docs fix; spicy-regs local candidate |
| H11 | **Fixed locally; public regeneration pending:** CFR part suffixes truncated | 336 of 319,186 public granule identifiers explicitly contain lettered parts such as `1203a`, `1203b` and `15a`; the host drops their letters | Distinct parts acquire the same citation | Preserve the explicit suffix; do not extend numeric-prefix hierarchy inference | `cb75404`, four real-ID regressions and complete identifier census; `data-validation-sprint-2026-09-21/cfr-fix-review.json` | spicy-regs |
| H12 | **Fixed locally; historical/public adoption open:** Federal Register number-only identity lost date-distinct records | Fresh native daily responses contain two different `00-111` documents on 2000-01-14 and 2000-01-18; the audited public output retains only the latter and the original merge partitions on number alone | A distinct source document disappears and number-only cross-source references are ambiguous | Use date plus number throughout merge and consumer identity; preserve ambiguous source references and backfill the lost historical record | Two complete native daily responses, public survivor and current-code trace in the [regulatory audit](data-validation-regulatory-2026-09-21.md) | spicy-regs; source citation consumers |
| H13 | **Fixed in versioned local output; wider rebuild open:** old court body rows advertise text they do not retain | 73 of 250,000 retained local rows report positive HTML text but have neither stored body field; the original code counted eight variants and retained two | Retrieval returns no text despite positive availability metadata | Retain an exact original dump witness, then review a versioned representation of all source variants | Output/code counterexample in the [other-source audit](data-validation-other-2026-09-21.md); original historical field bytes remain unvalidated | spicy-regs |
| H14 | **Retained repair verified; public backfill open:** public regulatory rows omit fields current readers preserve | Matched ACF native/public rows with equal modification timestamps lose document renditions, Federal Register numbers and withdrawal facts; docket RIN losses are separately counted, including literal `Not Assigned` | Evidence retrieval and regulatory joins fail despite available source facts | Adopt and backfill existing mappings from retained raw bytes into a pinned generation; separate source placeholders from usable identifiers | 391 docket and 546 document pairs; 9,289 current conversion checks; exact same-revision witness in `independent-witness-review-v2.json` and the regulatory report | spicy-regs adoption; source conversion already preserves inspected fields |
| H15 | Lifecycle grouping collapses unknown identities and excludes proposal groups | The 19 retained unknown-docket agency groups represent 48 proposals; 5,132 is the wider pre-filter population. Another 647 non-null proposal groups fail the earliest-final/earliest-proposal ordering condition | Rows cannot be treated as a census of distinct regulatory lifecycles or definitive stuck/completed events | Retain participating document IDs and unresolved outcomes; define and qualify pairing before duration or status analysis | Independent SQL, pinned inputs and exact denominators in `regulatory/lifecycle-null-grain-review-verified.json`; [regulatory audit](data-validation-regulatory-2026-09-21.md) | spicy-regs |
| H16 | **Fixed in code; initial backfill open:** source failure became empty or partial lobbying output | A scheduled unfiltered request returned HTTP 400 and published zero rows; independent old-code replay also turns a later HTTP 503 into partial output | A successful job and schema-valid generation can falsely imply source absence/completeness | Reader `7551f63` fails before output/publication; invalid family withdrawn; schedule paused pending bounded seed/resume | Two retained live pages, 42 source/output cells, original/repaired failure replay and 2,107-test gate in §2.0 | spicy-regs |

The full validation scorecard also records interpretation, completeness and
usefulness limits beyond H11–H15: generic report headings labeled as agencies,
hearing-block association errors, lost meeting addresses and nested committee
identities, imputed date precision, missing citation spellings, unproduced
outputs, limited body coverage and unsafe aggregation across one-to-many joins.
Those findings remain open with evidence and next actions in the linked sprint;
inventory completion does not close them.

Also established in the same pass, and not a defect: a regex scan of every
string column of every sampled file found four credential-shaped values, all
the same public agency URL carrying a `key=` query parameter inside a document
title and two abstracts. Publisher content, not a leaked credential; nothing
was redacted.

### 2.7 Candidates from the capture-schema review (filed, unbuilt)

The outside read of DocumentCapture ([document-capture-review-codex-2026-09-20.md](document-capture-review-codex-2026-09-20.md)) kept the architecture and named what the owner's goal still lacks. Each is a candidate until built and measured.

| # | Candidate | Evidence | Why it matters | Fix | Proof | Home |
|---|---|---|---|---|---|---|
| G1 | **Closed 2026-09-20:** reversible capture JSON/XML | `src/spicy_docs/schemas/document_capture/xml.py`; namespace `urn:spicy-docs:document-capture:xml:1`; [decision](../decisions.md#reversible-capture-xml-names-the-captures-structures) preceded implementation | The capture composes back to JSON or XML with every stated field retained | Package `encode_capture` / `decode_capture` use capture property names and explicit types, preserving node order, spans, cells, extensions and provenance. No value normalization; forbidden XML characters are escaped reversibly. Publisher vocabulary remains a separate lossy projection; CFR output is unchanged | [Zero-request measurement](document-capture-xml-roundtrip-2026-09-20.md): all seven retained captures plus a fresh Senate adapter result pass full structural equality; 19 XML mutations detected and four injected encoder faults fail positive fixture tests. XML is 2.36× JSON overall; parent/profile/invariants pass both ways; gate 7,125 passed. This establishes capture preservation, not missing metadata, extraction correctness or publisher/PDF reconstruction | spicy-docs (mapping, code and fixtures); Rulespec parent/profile schemas unchanged |
| G2 | **Closed for the measured Senate sample:** PDF table observations enter the capture; G1 is closed separately and G3 (required provenance) remains open | `tools/analysis/document_capture_pdf_tables.py` composes the line adapter with `PageResult.tables`; the unchanged parent represents `table`/`row`/`cell` | Retains observed cells and geometry, including text that cannot be reconciled with the line stream | Reusable adapter plus bounded `senate-expenditures-pdf` profile; 87/87 empty cells stay distinct from 3,905/3,905 missing positions; 505/2,267 cells without observed boxes are flagged | [Offline measurement](document-capture-pdf-tables-2026-09-20.md): 160 pages, 139 tables, 673/673 ruled rows and 2,267/2,267 cells agree with the reference recomputed from retained observations; both readings share PyMuPDF, so this checks preservation and cannot catch shared extraction defects; capture-only 0/2,267, contract-only 0/2,267. Of 2,180 nonempty cells, 1,505 have exact spans and 675 retain text with unresolved reasons. Parent/profile/invariants pass; mutation checks detect disagreement. Zero requests; unread pages and other families remain unmeasured | spicy-docs |
| G3 | **Shared v2 implementation complete; source adoption and missing evidence remain open** (RS1 in §2.0 supersedes the original shared-field request below) | [Provenance measurement and owner handoff](document-capture-provenance-2026-09-20.md); package `check_provenance` and seven committed captures | Missing provenance now produces family-specific findings even when the pinned parent accepts it | Converter retains acquisition/MODS references, package/granule pairs, available full timestamps, source-native metadata, page boxes and decisions for generated nodes. Rulespec must add shared source records, derived-artifact relationships, conditional provenance requirements and rule-version bindings; pinned schemas remain unchanged | Zero requests; committed before/after field completeness 117/199 → 164/199; omission and measurement mutations fail. Eleven findings remain: two date-only timestamps, three missing MODS records, six unobserved Senate cell boxes. Gate: 7,231 passed | spicy-docs local work complete; rulespec schema work and unavailable source evidence remain |
| G4 | **Closed in SpicyDocs 2026-09-20; shared Rulespec representation pending** | [Public-law byte association and proof](document-capture-provenance-2026-09-20.md); `archiveMember` in the permitted USLM extension | The XML member and ZIP archive now name their own bytes | Artifact retains the XML path, digest, size and media type. Extension retains ZIP URL, path, digest, size, media type and full acquisition timestamp plus exact member path; Rulespec must supply the shared archive/member definition before this can move into the parent | Retained ZIP and unique member verified offline; generic locator/receipt checks and ZIP/member mutations detect wrong associations. The original ZIP-URL/XML-digest mutation fails; no parent or meta-schema pin changed | spicy-docs correction complete; rulespec shared schema pending |
| G5 | **Closed 2026-09-20 and pushed.** The validator did not enforce its declared tree guarantee | parents-precede-children is specified but the checker builds a dictionary of all nodes first; no id-uniqueness check | A malformed capture validates | Add the checks and negative fixtures | Mutation fixtures that fail | rulespec **Closed 2026-09-20** in rulespec (merge 21693e0a on its `main`, pushed to `Formspec-Labs/rulespec` 2026-09-20 with the eleven DocumentCapture commits behind it). Both checks are findings now, `parent-not-earlier` and `duplicate-node-id`, each reproduced first by a negative fixture that validated before the change and pinned by a test asserting the finding is the only one and that repairing the defect clears it; no schema moved, and the package version is left for a maintainer bump. `make test-document-capture` passes 14 tests; four pre-existing `test-audits` failures on rulespec's own main are untouched and unrelated. |
| G6 | **Closed for 1.0.14 adoption; opt-in v2 remains separate:** the following 1.0.13/temporary-loader state is historical, superseded by G5 owner-package adoption in §2.0 | `PINS.json`, spicy-regs `vendor/`; the wheel-equality test skips | The temporary vendored loader stays in place | Release 1.0.14, move both pins, delete the loader when equality holds | The wheel-equality test running, not skipping | user (rulespec release), then spicy-docs and spicy-regs |

The same review noted that the six profiles compose from the parent without copying it, that the ownership boundary holds, and that the newer PDF-family tables bypass DocumentCapture by design; none of that is a gap.

## 3. Reconstruction: GovernmentXML as a spicy-docs subpackage

The external proposal's architecture is right and about half of it is
already here. The resolver is this repository's acquisition layer; the
evidence layer is `extraction/` with `raw` retained; the rule discipline is
the interpretation package's contract; DocSpec is a sibling. What does not
exist is the profile, the structural parser, the serializer, schema
validation, the benchmark and the review report. Those become
`src/spicy_docs/reconstruction/`, behind a `reconstruct` extra (lxml for
DTD and XSD validation; nothing else in core), for these reasons: the
resolver and the evidence model are not rebuilt, one release train carries
it to spicy-regs, and the split stays intact (spicy-docs parses, spicy-regs
hosts the derivative rows with `derivation = reconstructed` on every one).

### 3.1 The corpus decision

The external draft picks annual CFR first because it has a guide and XML.
That makes it a paired benchmark only: every edition GovInfo serves has XML,
so reconstructing it is the experiment the draft itself calls "not the
primary production use case". The census found the PDF-only remainder is
reports and opinions with no target vocabulary. The one family with a real
corpus, a real target and a text rendition to reconstruct from is **bill
text from the 103rd to the 112th Congress**: HTML and PDF only, ten
Congresses, the bill DTD as target, DeltaTrack as the consumer, and section
diffs across versions as the outcome nobody has today.

So: CFR stays the controlled benchmark (hide the XML, reconstruct, compare,
blind splits grouped by edition), and pre-113th bills are the deployment
corpus and the second profile. Reconstruction reads the **HTML rendition**,
not the PDF, wherever it exists; the CRPT measurement (PDF text splits about
1,870 words per report and keeps no account rows where the HTML keeps 841)
is the reason, and it is the retrieve-first principle applied one level
down.

### 3.2 Modules

As landed (merge 1580899): `profiles.py` is one module holding `CFR_PROFILE`
only, the `bill_dtd` profile is not written, `review.py` does not exist, and
`classify_and_attach` is an injected seam with abstention, not a wired model
call. The table below is the plan as proposed.

| Module | Responsibility | Reuses |
|---|---|---|
| `reconstruction/profiles/` | One versioned profile per family: `cfr` (GPO CFR XML guide, pinned DTD), `bill_dtd` (xml.house.gov bill DTD, the version the 113th-era files declare). A profile is a frozen record: applicability rule, schema bundle digests, interpretation rules as a table, serializer, validation rules, fixture references. Three rule kinds kept distinct: schema requirement, guide convention, project heuristic | `interpretation/`'s rule-table shape |
| `reconstruction/evidence.py` | The evidence-linked document model: blocks with spans, page or line coordinates, style observations, `TableObservation` (B5), reading-order candidates; unresolved regions survive with an issue attached | `extraction/model.py`, `body_text` |
| `reconstruction/parse.py` | Deterministic structural parsing: numbering, indentation, typography and context jointly propose the tree; every node carries `evidence_refs` and a `decision` naming the rule; where rules cannot decide, a bounded `classify_and_attach` model call chooses between evidence-backed alternatives with abstention, never rewriting text | `interpretation/model_call.py`, `extraction/gemini` |
| `reconstruction/serialize.py` | Deterministic XML in the target vocabulary from the document model; a sidecar source map from XML paths to evidence; no custom attributes in the official vocabulary; generated ids marked as generated | `sources/xml.py` conventions |
| `reconstruction/validate.py` | Five checks, each a separate finding: schema validity (lxml against the pinned bundle, catalog-resolved, no network), content fidelity (normalized text equality with the reversible normalization stated), structural fidelity (boundaries, hierarchy, footnote attachment), coverage (every region classified), acceptance gates | `gpo_normalize`'s record shape for counts |
| `reconstruction/review.py` | An HTML report: PDF or HTML page beside the tree, node to evidence; issues listed; no editor until error analysis asks for one | none |
| `tools/analysis/reconstruction_benchmark.py` | The paired corpus builder and scorer: development, tuning and blind splits grouped by edition; text precision and recall, hierarchy F1, critical-discrepancy count, acceptance coverage, cost per accepted document | `tools/analysis/legislative_data_map.py`'s evidence discipline |

### 3.3 Gates

Engineering targets, not results: all accepted XML passes its pinned
schema; text precision and recall at or above 99.9% on born-digital
material with the normalization stated; hierarchy F1 at or above 0.98 on
supported structures; no unresolved change to a number, date, negation or
provision marker in accepted output; every region accounted for; at least
70% acceptance without review in the declared slice; cost per accepted
document reported. A hundred clean documents still permit a 3% error rate
at a one-sided 95% bound, so the audit grows with the claim.

### 3.4 What reconstruction does not do

It does not promise byte recovery of lost XML, does not carry GovInfo's
signature onto derived files (the original is retained separately), does not
interpret legal effect or consolidate amendments, does not force reports into
a legislative schema, does not OCR scans in the pilot, and does not take
private documents.

## 4. Waves and gates

| Wave | Scope | Gate to pass |
|---|---|---|
| **1. Joins and hygiene** (days) | A1, A2, A3, A4, A12, A13, D4, E3, E5; triage E4's Federal Register drift | The four linkage columns non-NULL on fixtures; the map regenerated with every landed row `have`; the Senate index fixture parsed |
| **2. The rest of the map** (a week) | A5, A7, A8, A9, A10, A11, B2, B7, then A6's two measurements | Each new contract with a fixture record through the generic loop; the regulatory bridge answering `communication ↔ rule` on the fixture; the histogram deciding the requirements table |
| **3. Reconstruction pilot** (six to eight engineering weeks, one engineer, a part-time reviewer) | §3: the CFR benchmark first, then the `bill_dtd` profile on pre-113th HTML, B5's table observations alongside | The blind-split numbers against §3.3; ten Congresses of sections and diffs hosted with `derivation = reconstructed` |
| **4. Measured operation** (ongoing) | D1, C1, C2, C3, C4, B6, B3, D2 | Coverage statements carrying real numbers; model tables with provenance rows; the normalizer's corpus table |
| **Standing** | E1, E2 (user files; pins move when upstream ships), D3 (after the upstream merge), releases 0.21.x to 0.22 with each wave, the spicy-regs pull request when the user names the branch | Issue numbers in the doc; pins on a release; the catalog re-vendored |

Order within a wave follows value per request: everything in wave 1 needs
no new publisher and no new credential.

## 5. What stays out

BillTrax itself (external; its delete column is a record, not work). HTML
scraping of the sites the map rejected. A universal PDF-to-XML converter or
"supply any XSD". OCR of scans until a consumer names the corpus. Any push
to the upstream spicy-regs origin without the user naming the branch.
Licensing as a selection criterion: the user ruled results only, and the
PyMuPDF pipeline stays because it measured best.

## 6. Verification

Every check through the project's own runner, never a bare binary:

```sh
# spicy-docs
./scripts/check
uv run --frozen python -m tools.analysis.legislative_data_map --offline \
  --output docs/research/legislative-data-map-2026-09-18.json \
  --map docs/research/legislative-data-map-2026-09-18.md
uv run --frozen pytest -q -m integration   # weekly, reported not gated

# spicy-regs, fork branch
uv run --frozen pytest -q
uv run --frozen ruff check .
uv run --frozen spicy-regs-dict check
uv run --frozen spicy-regs-dict generate && git status --short docs/tables data_dictionary
```
